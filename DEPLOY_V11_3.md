# BoardMate Arcade v11.3 배포

## 이번 버전 핵심
- 포크노바 본체를 사용자 제공 `pocketnova-v3.zip`으로 교체
- 포크노바 1인플 추가
- 포크노바 온라인 BoardMate 상태 브리지 재연결
- DB 변경 없음

## 배포 순서
1. 현재 GitHub Pages 파일을 백업합니다.
2. `boardmate_arcade_v11_3_handoff/` 안의 파일을 사이트 루트에 덮어씁니다.
3. **운영 중인 `config.js`의 실제 Supabase URL/anon key는 기존 값을 유지**하세요. ZIP의 샘플/현재 파일로 의도치 않게 바꾸지 마세요.
4. 이번 버전은 Supabase SQL을 실행할 필요가 없습니다.
5. 브라우저 강력 새로고침(Ctrl+Shift+R).

## 우선 테스트
### 1인플
- 홈 → 1인플 → 포크노바
- 1명으로 시작되는지
- 시작 손패 4장 선택 후 게임 진입
- 새로고침 → 이어하기 표시

### 다인플 포크노바
- **새 방을 만들어 테스트**합니다. 이전 포크노바 상태 kind와 호환하지 않습니다.
- 2명 이상 입장 → 방장 시작
- 방장이 지도/시작 손패 드래프트 완료
- 현재 차례 플레이어만 조작 가능 / 다른 좌석은 보기 전용
- 한 턴 진행 후 다른 브라우저에 상태 반영
- 새로고침/재접속 후 상태 복구
- 게임 종료 시 ELO 결과 제출

## SQL 관련
- `supabase.sql`: 전체 DB 기준본, 계속 유지/사용. 새 프로젝트 구축 또는 전체 재구축 시 사용.
- `SUPABASE_RPC_FIX_v11_1.sql`: 과거 방 생성/게임 취소 RPC schema-cache 장애 복구용 패치. 현재 운영에서 이미 정상이라면 재실행 불필요.
- v11.3 신규 SQL 없음.
