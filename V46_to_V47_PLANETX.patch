--- /mnt/data/v46_base_for_diff/online-planetx.html	2026-09-10 00:28:07.000000000 +0000
+++ online-planetx.html	2026-09-10 00:50:10.691393847 +0000
@@ -4,7 +4,7 @@
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
 <title>BoardMate Online · 행성 X를 찾아서</title>
-<!-- BoardMate Arena v11.4.46 · Planet X circular private record sheet -->
+<!-- BoardMate Arena v11.4.47 · Planet X record-sheet layout toggle + emoji footnote -->
 <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
 <script src="./config.js"></script>
 <style>
@@ -63,6 +63,10 @@
 .record-sheet-tip{text-align:center;font-size:10px;color:#a99bc7;margin:0 0 5px;line-height:1.45}
 @media(max-width:700px){.record-wheel-wrap{padding-inline:0}.record-wheel,.record-wheel.expert{width:min(100%,560px)}}
 @media(max-width:520px){.record-wheel{width:min(100%,430px)}.record-wheel.expert{width:min(100%,455px)}.record-mark{width:clamp(15px,4.7vw,22px);height:clamp(15px,4.7vw,22px)}.record-mark-icon{font-size:clamp(9px,3.2vw,12px)}.record-mark .mkstate{right:-4px;bottom:-4px;min-width:11px;height:11px;font-size:7px}.record-wheel.expert .record-mark{width:clamp(12px,4vw,18px);height:clamp(12px,4vw,18px);border-radius:5px}.record-wheel.expert .record-mark-icon{font-size:clamp(8px,2.7vw,10px)}.record-wheel.expert .record-mark .mkstate{right:-3px;bottom:-3px;min-width:9px;height:9px;font-size:6px}.record-sector-no{font-size:10px;min-width:22px;height:20px}.record-wheel.expert .record-sector-no{font-size:8px;min-width:18px;height:17px}.record-wheel-center strong{font-size:10px}.record-wheel-center span{font-size:8px}.record-wheel-center small{font-size:7px}}
+/* v11.4.47 · record-sheet circle/square switch + emoji footnote */
+.record-layout-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:14px;margin-bottom:7px;flex-wrap:wrap}.record-layout-head h3{margin:0}.record-layout-switch{display:inline-flex;gap:3px;padding:3px;border:1px solid #4b3d70;border-radius:12px;background:#100f22}.record-layout-switch button{border:1px solid transparent;border-radius:9px;padding:6px 9px;background:transparent;color:#a99bc7;font-size:10px;font-weight:950;cursor:pointer}.record-layout-switch button:hover{color:#efe9ff;background:#241d3c}.record-layout-switch button.active{border-color:#8d72c8;background:#35285a;color:#fff;box-shadow:0 0 0 1px #b99cff22}.record-layout-switch button:focus-visible{outline:2px solid var(--gold);outline-offset:2px}.record-emoji-footnote{margin:9px 0 2px;padding:8px 10px;border:1px dashed #55436f;border-radius:10px;background:#141225;color:#b9abc9;font-size:9px;line-height:1.55}.record-emoji-footnote b{color:#e0d5ef}.record-emoji-items{display:flex;gap:5px 10px;flex-wrap:wrap;margin-top:3px}.record-emoji-items span{white-space:nowrap}.square-record-wrap{margin-top:8px}.square-record-wrap .note-grid{grid-template-columns:repeat(3,minmax(0,1fr))}.square-record-wrap .note-cell>b{font-size:12px;color:#f2ecff}.square-record-wrap .mark{cursor:pointer}.square-record-wrap .mark:focus-visible{outline:2px solid var(--gold);outline-offset:1px}
+@media(max-width:700px){.square-record-wrap .note-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
+@media(max-width:420px){.record-layout-switch{width:100%;display:grid;grid-template-columns:1fr 1fr}.record-layout-switch button{width:100%}.square-record-wrap .note-grid{grid-template-columns:1fr 1fr}}
 
 </style>
 </head>
@@ -119,6 +123,9 @@
 let theoryPlacing=false,theoryAutoKey='';
 let privateSaveChain=Promise.resolve();
 let uiRecordOpen=false;
+const RECORD_LAYOUT_KEY='boardmate:planetx:record-layout';
+function initialRecordLayout(){try{return localStorage.getItem(RECORD_LAYOUT_KEY)==='square'?'square':'circle'}catch{return'circle'}}
+let uiRecordLayout=initialRecordLayout();
 let researchTopics={};
 let priv={hints:[],season:'',difficulty:'',observations:[],marks:{},research:{}};
 
@@ -165,7 +172,7 @@
     s.theoryRound=Number(s.theoryRound||0)+1;s.phase='theory-select';s.theoryPlaceIndex=0;s.theoryQueue=[];s.current=activeSeat(s);logPublic(s,`논문 단계 ${s.theoryRound} 시작`);return s;
   }
 }
-function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.46',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
+function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.47',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
 
 function captureScroll(){const inner={};document.querySelectorAll('[data-scroll-key]').forEach(el=>{inner[el.dataset.scrollKey]={top:el.scrollTop,left:el.scrollLeft}});return{x:window.scrollX,y:window.scrollY,inner}}
 function restoreScroll(snap){if(!snap)return;requestAnimationFrame(()=>{window.scrollTo(snap.x,snap.y);document.querySelectorAll('[data-scroll-key]').forEach(el=>{const p=snap.inner?.[el.dataset.scrollKey];if(p){el.scrollTop=p.top;el.scrollLeft=p.left}})})}
@@ -252,13 +259,17 @@
 function finalPanel(){const item=state.finalQueue?.[state.finalIndex];if(!item)return`<button id="finishReveal" class="btn gold">정답 공개 · 점수 계산</button>`;if(item.seat!==mySeat())return`<div class="notice"><b>${playerNameHtml(item.seat)}</b>의 마지막 점수 기회입니다.<br>선두보다 ${item.distance}칸 뒤였습니다.</div>`;const max=item.distance<=3?1:2;return`<div class="notice"><b>마지막 점수 기회</b><br>선두보다 ${item.distance}칸 뒤 · 논문 ${max}개 제출 <b>또는</b> 행성 X 찾기를 시도할 수 있습니다. 이때 시간은 이동하지 않습니다.</div><div class="actions"><button id="finalTheory" class="btn green">논문 최대 ${max}개 제출</button><button id="finalLocate" class="btn gold">행성 X 찾기</button></div>`}
 function privateObservationPanel(){const rows=priv.observations||[];return`<section class="private-quick"><div class="private-quick-head"><h3>🔒 내 비공개 조사 기록</h3><span class="badge">${rows.length}건</span></div><div class="record-tip">조사 결과가 최신순으로 바로 쌓입니다. 보드 아래에서 계속 확인할 수 있습니다.</div><div class="obs quick-obs" data-scroll-key="private-observations" style="margin-top:7px">${rows.map(o=>`<div class="line"><b>${esc(o.at)} · ${esc(o.type)}</b><br>${esc(displayKo(o.text))}</div>`).join('')||'<div class="sub">아직 조사 기록이 없습니다.</div>'}</div></section>`}
 function recordPoint(angleDeg,radiusPct){const rad=angleDeg*Math.PI/180;return{x:50+Math.sin(rad)*radiusPct,y:50-Math.cos(rad)*radiusPct}}
-function privatePanel(){const n=sectorCount(),expert=n===18,step=360/n,sectorOneAngle=expert?100:105,allowedComets=new Set(cometSectors());const radii=expert?{number:46.0,x:39.4,empty:34.7,gas:30.0,dwarf:25.3,asteroid:20.6,comet:15.9}:{number:46.0,x:39.7,empty:35.0,gas:30.3,dwarf:25.6,asteroid:20.9,comet:16.2};const spokes=Array.from({length:n},(_,i)=>{const a=sectorOneAngle+(i*step)-(step/2);return`<i class="record-spoke" style="transform:translateX(-50%) rotate(${a+180}deg)"></i>`}).join('');const sectorMarks=Array.from({length:n},(_,i)=>i+1).map(sec=>{const angle=sectorOneAngle+(sec-1)*step,num=recordPoint(angle,radii.number),objects=['x','empty','gas','dwarf','asteroid',...(allowedComets.has(sec)?['comet']:[])];return`<span class="record-sector-no" style="left:${num.x.toFixed(3)}%;top:${num.y.toFixed(3)}%">${sec}</span>${objects.map(o=>{const p=recordPoint(angle,radii[o]),v=priv.marks?.[sec]?.[o]||'',label=objLabel(o);return`<button type="button" class="record-mark ${o==='comet'?'comet-mark ':''}${v}" style="left:${p.x.toFixed(3)}%;top:${p.y.toFixed(3)}%" title="${esc(label)} · 미정/없음/있음 전환" aria-label="${sec}섹터 ${esc(label)} 기록" data-mark="${sec}:${o}"><span class="record-mark-icon">${OBJICON[o]}</span><span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}`}).join('');const modeLabel=expert?'전문가 · 18섹터':'표준 · 12섹터';return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">계정에 저장된 시작 정보가 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><h3 style="margin-top:14px">📝 원형 섹터 기록지</h3><div class="record-sheet-tip">실물 기록지처럼 바깥쪽 섹터 번호에서 안쪽으로 개체 아이콘을 배치했습니다. 아이콘을 누르면 미정 → × 없음 → ✓ 있음으로 바뀝니다. 혜성은 가능한 섹터에만 표시됩니다.</div><div class="record-wheel-wrap"><div class="record-wheel ${expert?'expert':'standard'}" style="--sector-count:${n}">${spokes}${sectorMarks}<div class="record-wheel-center"><strong>섹터 메모</strong><span>${SEASON_KO[priv.season]||modeLabel}</span><small>${modeLabel}<br>× 없음 · ✓ 있음</small></div></div></div>`}
+function recordEmojiFootnote(){return`<div class="record-emoji-footnote"><b>※ 이모지 각주</b> · 이모지는 화면에서 개체를 빠르게 구분하기 위한 표기입니다.<div class="record-emoji-items"><span>${OBJICON.asteroid} 소행성</span><span>${OBJICON.comet} 혜성</span><span>${OBJICON.dwarf} 왜소행성</span><span>${OBJICON.gas} 가스 구름</span><span>${OBJICON.empty} 비어 보임</span><span>${OBJICON.x} 행성 X</span><span>× 없음</span><span>✓ 있음</span></div></div>`}
+function squareRecordSheet(){const allowedComets=new Set(cometSectors());const marks=Array.from({length:sectorCount()},(_,i)=>i+1).map(sec=>{const objects=['asteroid',...(allowedComets.has(sec)?['comet']:[]),'dwarf','gas','empty','x'];return`<div class="note-cell ${allowedComets.has(sec)?'comet-possible':''}"><b>${sec}</b>${allowedComets.has(sec)?'<span class="comet-possible-label">🌠 혜성 가능</span>':''}<div class="mark-row">${objects.map(o=>{const v=priv.marks?.[sec]?.[o]||'',label=objLabel(o);return`<button type="button" class="mark ${v}" title="${esc(label)} · 미정/없음/있음 전환" aria-label="${sec}섹터 ${esc(label)} 기록" data-mark="${sec}:${o}">${OBJNOTE[o]}<span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}</div></div>`}).join('');return`<div class="square-record-wrap"><div class="record-sheet-tip">기존 네모 기록지입니다. 아이콘/항목을 누르면 미정 → × 없음 → ✓ 있음으로 바뀝니다. 혜성은 가능한 섹터에만 표시됩니다.</div><div class="note-grid">${marks}</div></div>`}
+function circularRecordSheet(){const n=sectorCount(),expert=n===18,step=360/n,sectorOneAngle=expert?100:105,allowedComets=new Set(cometSectors());const radii=expert?{number:46.0,x:39.4,empty:34.7,gas:30.0,dwarf:25.3,asteroid:20.6,comet:15.9}:{number:46.0,x:39.7,empty:35.0,gas:30.3,dwarf:25.6,asteroid:20.9,comet:16.2};const spokes=Array.from({length:n},(_,i)=>{const a=sectorOneAngle+(i*step)-(step/2);return`<i class="record-spoke" style="transform:translateX(-50%) rotate(${a+180}deg)"></i>`}).join('');const sectorMarks=Array.from({length:n},(_,i)=>i+1).map(sec=>{const angle=sectorOneAngle+(sec-1)*step,num=recordPoint(angle,radii.number),objects=['x','empty','gas','dwarf','asteroid',...(allowedComets.has(sec)?['comet']:[])];return`<span class="record-sector-no" style="left:${num.x.toFixed(3)}%;top:${num.y.toFixed(3)}%">${sec}</span>${objects.map(o=>{const p=recordPoint(angle,radii[o]),v=priv.marks?.[sec]?.[o]||'',label=objLabel(o);return`<button type="button" class="record-mark ${o==='comet'?'comet-mark ':''}${v}" style="left:${p.x.toFixed(3)}%;top:${p.y.toFixed(3)}%" title="${esc(label)} · 미정/없음/있음 전환" aria-label="${sec}섹터 ${esc(label)} 기록" data-mark="${sec}:${o}"><span class="record-mark-icon">${OBJICON[o]}</span><span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}`}).join('');const modeLabel=expert?'전문가 · 18섹터':'표준 · 12섹터';return`<div class="record-sheet-tip">실물 기록지처럼 바깥쪽 섹터 번호에서 안쪽으로 개체 아이콘을 배치했습니다. 아이콘을 누르면 미정 → × 없음 → ✓ 있음으로 바뀝니다. 혜성은 가능한 섹터에만 표시됩니다.</div><div class="record-wheel-wrap"><div class="record-wheel ${expert?'expert':'standard'}" style="--sector-count:${n}">${spokes}${sectorMarks}<div class="record-wheel-center"><strong>섹터 메모</strong><span>${SEASON_KO[priv.season]||modeLabel}</span><small>${modeLabel}<br>× 없음 · ✓ 있음</small></div></div></div>`}
+function privatePanel(){return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">계정에 저장된 시작 정보가 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><div class="record-layout-head"><h3>📝 섹터 기록지</h3><div class="record-layout-switch" role="group" aria-label="기록지 모양"><button type="button" class="${uiRecordLayout==='circle'?'active':''}" aria-pressed="${uiRecordLayout==='circle'}" data-record-layout="circle">◯ 원형</button><button type="button" class="${uiRecordLayout==='square'?'active':''}" aria-pressed="${uiRecordLayout==='square'}" data-record-layout="square">▦ 기존 네모</button></div></div>${uiRecordLayout==='square'?squareRecordSheet():circularRecordSheet()}${recordEmojiFootnote()}`}
 
 function scorePanel(){const scores=state.scores||[];return`<h2>🏆 최종 점수</h2><table class="score-table"><thead><tr><th>플레이어</th><th>논문</th><th>리더</th><th>행성 X</th><th>합계</th></tr></thead><tbody>${scores.map(x=>`<tr><td style="border-left:4px solid ${playerColor(x.seat)}">${x.rank===1?'🏆 ':''}<span style="color:${playerColor(x.seat)}">●</span> ${esc(memberName(x.seat))} <span class="player-color-name" style="--pc:${playerColor(x.seat)}">${playerColorName(x.seat)}</span></td><td>${x.theory}</td><td>${x.leader}</td><td>${x.x}</td><td><b>${x.total}</b></td></tr>`).join('')}</tbody></table>${state.sharedTie?'<div class="notice" style="margin-top:10px">공식 타이브레이커 후에도 완전 동점이라 공동 승리입니다. 이 경우 BoardMate ELO는 자동 반영하지 않습니다.</div>':''}${endPanel('게임 종료',scores.filter(x=>x.rank===1).map(x=>memberName(x.seat)).join(', ')+' 승리')}`}
 
 function bindGameControls(){
  document.querySelector('#survey')?.addEventListener('click',openSurvey);document.querySelector('#target')?.addEventListener('click',openTarget);document.querySelector('#research')?.addEventListener('click',openResearch);document.querySelector('#locate')?.addEventListener('click',()=>openLocate(false));document.querySelector('#chooseTheory')?.addEventListener('click',openTheoryChoice);document.querySelector('#finalTheory')?.addEventListener('click',openFinalTheory);document.querySelector('#finalLocate')?.addEventListener('click',()=>openLocate(true));document.querySelector('#finishReveal')?.addEventListener('click',async()=>{const s=cloneState();s.phase='reveal';await commit(s,'정답 공개')});
  const sheet=document.querySelector('.private-sheet-box');if(sheet){uiRecordOpen=sheet.open;sheet.addEventListener('toggle',()=>{uiRecordOpen=sheet.open})}
+ document.querySelectorAll('[data-record-layout]').forEach(b=>b.addEventListener('click',()=>{const next=b.dataset.recordLayout==='square'?'square':'circle';if(next===uiRecordLayout)return;uiRecordLayout=next;uiRecordOpen=true;try{localStorage.setItem(RECORD_LAYOUT_KEY,next)}catch{}render()}));
  document.querySelectorAll('[data-mark]').forEach(b=>b.onclick=()=>{const [sec,o]=b.dataset.mark.split(':');priv.marks[sec]??={};const cur=priv.marks[sec][o]||'';const next=cur===''?'no':cur==='no'?'yes':'';priv.marks[sec][o]=next;savePrivate();b.classList.remove('no','yes');if(next)b.classList.add(next);const st=b.querySelector('.mkstate');if(st)st.textContent=next==='no'?'×':next==='yes'?'✓':''});
 }
 function optionObjects(includeEmpty=false){const xs=['asteroid','comet','dwarf','gas'];if(includeEmpty)xs.push('empty');return xs.map(o=>`<option value="${o}">${objLabel(o)}</option>`).join('')}
--- /mnt/data/v46_base_for_diff/sw.js	2026-09-10 00:24:40.000000000 +0000
+++ sw.js	2026-09-10 00:50:10.691558882 +0000
@@ -1,4 +1,4 @@
-const CACHE='boardmate-shell-v11.4.46';
+const CACHE='boardmate-shell-v11.4.47';
 self.addEventListener('install',()=>self.skipWaiting());
 self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('boardmate-shell-')&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
 self.addEventListener('fetch',event=>{
--- /mnt/data/v46_base_for_diff/index.html	2026-09-10 00:24:40.000000000 +0000
+++ index.html	2026-09-10 00:50:10.691616207 +0000
@@ -13,6 +13,6 @@
   <div id="app"></div>
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   <script src="./config.js"></script>
-  <script type="module" src="./app.js?v=46"></script>
+  <script type="module" src="./app.js?v=47"></script>
 </body>
 </html>
--- /mnt/data/v46_base_for_diff/HANDOFF_VERSION.txt	2026-09-10 00:24:40.000000000 +0000
+++ HANDOFF_VERSION.txt	2026-09-10 00:50:10.691642456 +0000
@@ -1 +1 @@
-11.4.46
+11.4.47
