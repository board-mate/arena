# Board Mate Calico V4 changes

## 변경 사항
- 디자인 타일 3개 선택 화면에 실제 플레이어 보드 미리보기 추가
- 선택 순서(위 → 왼쪽 아래 → 오른쪽)를 보드 슬롯 위에 표시
- 타일 배치 후 별도 토큰 선택/완료 버튼 제거
- 타일 배치 즉시 단추/고양이 조건 자동 판정
- 시장 타일 클릭으로 바로 손패 보충
- 시장 선택 후 `턴 종료` 버튼으로 다음 플레이어 진행
- 현재 턴 전체를 `↶ 현재 차례 되돌리기`로 undo
- 단추: 새로 형성된 동일 색 연결 그룹이 3개 이상이고 해당 그룹에 기존 같은 색 단추가 없을 때만 지급
- 고양이: 새로 형성된 동일 무늬 연결 그룹이 조건(크기/도형)을 만족하고 해당 패턴 영역에 이미 고양이가 없을 때만 지급
- 무지개 단추 조건 자동 판정
- 마지막 보드 칸을 채우면 시장을 고르지 않고 즉시 게임 종료
- 모바일에서 시장 3열 / 손패 2열 / 큰 터치 영역 / 보드 가로 스크롤 최적화

## 참고
현재 자동 토큰 판정은 플레이어 보드 내부 25칸을 기준으로 합니다. 실제 보드 가장자리의 인쇄 패치는 화면에는 보이지만 이번 V4 자동 판정에는 데이터화하지 않았습니다. 따라서 가장자리 패치 때문에 유효해지는 토큰은 다음 단계에서 보드별 edge-tile 데이터화를 추가하는 것이 좋습니다.

## 호환성
- Supabase SQL 변경 없음
- 기존 Board Mate Realtime 공통 모듈과 호환
- Calico 저장 버전: v4
