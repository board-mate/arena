# Calico V8 — 기존 진행 게임 자동 정정

## 목적
V7에서 내장한 4개 보드의 정적 인쇄 가장자리 데이터를 그대로 사용하면서, 이미 Supabase에 저장되어 있는 진행 중 게임을 새 코드로 다시 읽을 때 잘못 저장된 고양이 토큰을 자동 정정한다.

## 동작
- V7/V8 상태를 읽을 때 각 플레이어 보드의 모든 고양이 토큰을 다시 검사한다.
- 내부 5×5 타일 + 해당 보드의 인쇄 가장자리 22칸을 연결 그래프로 포함한다.
- 고양이의 선호 무늬, 연결 그룹 크기/도형 조건을 현재 보드 전체 기준으로 확인한다.
- 조건을 만족하지 않는 토큰은 제거하고 해당 고양이 재고를 1개 복구한다.
- 동일한 연결 그룹에 중복으로 저장된 고양이 토큰도 하나만 유지한다.
- 방 호스트가 수정된 상태를 읽은 경우 `repair-invalid-cat-tokens` 이유로 보정 상태를 다시 Arena에 저장한다.
- 일반 참가자는 우선 정정된 화면을 보고, 호스트가 재저장하면 방 전체 상태가 정정된다.

## 중요한 제한
토큰이 과거 어느 시점에 잘못 놓였는지에 대한 히스토리가 저장되어 있지 않기 때문에, V8은 **현재 보드 상태에서 조건을 만족하지 않는 토큰**을 정정한다. 이후 타일이 추가되어 현재 조건을 만족하는 경우에는 과거의 잘못된 배치를 소급해 식별할 수 없다.

## 사용 방법
기존 진행 방의 방장/참가자가 새 V8 파일을 배포한 사이트를 새로고침하면 된다. 호스트가 먼저 로드하면 보정 상태가 저장되어 이후 참가자 화면에도 반영된다.
