--- a/online-planetx.html
+++ b/online-planetx.html
@@ -4,7 +4,7 @@
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
 <title>BoardMate Online · 행성 X를 찾아서</title>
-<!-- BoardMate Arena v11.4.42 · Planet X record persistence + theory placement recovery -->
+<!-- BoardMate Arena v11.4.43 · Planet X theory same-sector rule fix -->
 <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
 <script src="./config.js"></script>
 <style>
@@ -125,7 +125,7 @@
     s.theoryRound=Number(s.theoryRound||0)+1;s.phase='theory-select';s.theoryPlaceIndex=0;s.theoryQueue=[];s.current=activeSeat(s);logPublic(s,`가설 단계 ${s.theoryRound} 시작`);return s;
   }
 }
-function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.42',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
+function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.43',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
 
 function captureScroll(){const inner={};document.querySelectorAll('[data-scroll-key]').forEach(el=>{inner[el.dataset.scrollKey]={top:el.scrollTop,left:el.scrollLeft}});return{x:window.scrollX,y:window.scrollY,inner}}
 function restoreScroll(snap){if(!snap)return;requestAnimationFrame(()=>{window.scrollTo(snap.x,snap.y);document.querySelectorAll('[data-scroll-key]').forEach(el=>{const p=snap.inner?.[el.dataset.scrollKey];if(p){el.scrollTop=p.top;el.scrollLeft=p.left}})})}
@@ -221,7 +221,7 @@
 function openLocate(finalMode){if((!finalMode&&!canAct())||(finalMode&&state.finalQueue?.[state.finalIndex]?.seat!==mySeat()))return;const n=sectorCount();modal(`<h2>✦ 행성 X 찾기</h2><p class="sub">행성 X 섹터와 양쪽 인접 섹터의 실제 개체를 모두 맞혀야 성공합니다. 시도한 섹터는 다른 플레이어에게 공개되지 않고 성공/실패만 공개됩니다.</p><div class="form-row"><div class="field"><label>행성 X 섹터</label><select id="lxSec">${optionSectors()}</select></div><div></div><div class="field"><label>왼쪽 인접 섹터</label><select id="lxLeft">${optionObjects(true)}</select></div><div class="field"><label>오른쪽 인접 섹터</label><select id="lxRight">${optionObjects(true)}</select></div></div><div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn gold" id="doLocate">위치 제출</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#doLocate').onclick=async()=>{const sec=Number(m.querySelector('#lxSec').value),left=m.querySelector('#lxLeft').value,right=m.querySelector('#lxRight').value;try{busy=true;const r=await rpc('boardmate_planetx_locate',{p_token:token(),p_room_id:roomId,p_sector:sec,p_left:left,p_right:right});addPrivate('행성 X 찾기',`섹터 ${sec} / 좌 ${objLabel(left)} / 우 ${objLabel(right)} → ${r.correct?'성공':'실패'}`);closeModal();if(finalMode)await finishFinalLocate(Boolean(r.correct));else await finishAction(5,'locate',`${memberName(mySeat())} · 행성 X 찾기 ${r.correct?'성공!':'실패'}`,Boolean(r.correct));toast(r.correct?'행성 X를 찾았습니다!':'행성 X 찾기 실패',r.correct?'':'error')}catch(e){toast(e.message||e,'error')}finally{busy=false}}})}
 async function finishAction(cost,type,publicText,located){const s=cloneState(),p=s.players[mySeat()];p.time+=cost;p.tie=++s.tieSeq;p.lastAction=type;if(type==='target')p.targetUsed++;logPublic(s,publicText);if(located){p.xScore=10;s.firstLocator={seat:mySeat(),time:p.time};s.finalQueue=s.players.filter(x=>x.seat!==mySeat()&&x.time<p.time).sort((a,b)=>a.time-b.time||a.tie-b.tie).map(x=>({seat:x.seat,distance:p.time-x.time}));s.finalIndex=0;s.phase=s.finalQueue.length?'final':'reveal';s.current=s.finalQueue[0]?.seat??mySeat();logPublic(s,'게임 종료 조건 발동 · 정확한 위치는 아직 공개되지 않습니다.');await commit(s,'행성 X 발견');return}await rotateToNextTurn(s);await commit(s,'액션 완료')}
 
-function openTheoryChoice(){const max=state.mode==='expert'?2:1;modal(`<h2>📄 가설 비공개 선택</h2><p class="sub">개체 종류는 서버의 비공개 테이블에 저장되며, 배치 후 다른 플레이어에게는 섹터와 토큰 소유자만 보입니다. 0개도 가능합니다.</p><div id="theoryRows">${Array.from({length:max},(_,i)=>`<div class="form-row"><div class="field"><label>${i+1}번째 개체</label><select data-th-obj><option value="">제출 안 함</option>${optionObjects(false)}</select></div><div class="field"><label>섹터</label><select data-th-sec>${optionSectors()}</select></div></div>`).join('')}</div><div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn green" id="saveTheoryChoice">비공개 선택 완료</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#saveTheoryChoice').onclick=async()=>{const os=[...m.querySelectorAll('[data-th-obj]')],ss=[...m.querySelectorAll('[data-th-sec]')],choices=os.map((o,i)=>o.value?{object:o.value,sector:Number(ss[i].value)}:null).filter(Boolean);if(new Set(choices.map(x=>x.sector)).size!==choices.length){toast('같은 단계에 같은 섹터로 두 가설을 동시에 낼 수 없습니다.','error');return}try{await rpc('boardmate_planetx_set_theory_choice',{p_token:token(),p_room_id:roomId,p_phase_id:state.theoryRound,p_choices:choices});closeModal();await sendRoomBroadcast('planetx_theory_ready',{phase:state.theoryRound});await updateTheoryStatus();toast('가설 선택이 비공개로 저장되었습니다.')}catch(e){toast(e.message||e,'error')}}})}
+function openTheoryChoice(){const max=state.mode==='expert'?2:1;modal(`<h2>📄 가설 비공개 선택</h2><p class="sub">개체 종류는 서버의 비공개 테이블에 저장되며, 배치 후 다른 플레이어에게는 섹터와 토큰 소유자만 보입니다. 0개도 가능합니다.</p><div id="theoryRows">${Array.from({length:max},(_,i)=>`<div class="form-row"><div class="field"><label>${i+1}번째 개체</label><select data-th-obj><option value="">제출 안 함</option>${optionObjects(false)}</select></div><div class="field"><label>섹터</label><select data-th-sec>${optionSectors()}</select></div></div>`).join('')}</div><div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn green" id="saveTheoryChoice">비공개 선택 완료</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#saveTheoryChoice').onclick=async()=>{const os=[...m.querySelectorAll('[data-th-obj]')],ss=[...m.querySelectorAll('[data-th-sec]')],choices=os.map((o,i)=>o.value?{object:o.value,sector:Number(ss[i].value)}:null).filter(Boolean);if(new Set(choices.map(x=>`${x.sector}:${x.object}`)).size!==choices.length){toast('같은 섹터에 같은 종류의 가설 토큰을 2개 제출할 수 없습니다.','error');return}try{await rpc('boardmate_planetx_set_theory_choice',{p_token:token(),p_room_id:roomId,p_phase_id:state.theoryRound,p_choices:choices});closeModal();await sendRoomBroadcast('planetx_theory_ready',{phase:state.theoryRound});await updateTheoryStatus();toast('가설 선택이 비공개로 저장되었습니다.')}catch(e){toast(e.message||e,'error')}}})}
 async function updateTheoryStatus(){if(!state||state.phase!=='theory-select')return;try{const st=await rpc('boardmate_planetx_theory_status',{p_token:token(),p_room_id:roomId,p_phase_id:state.theoryRound});if(!sameJson(state.theoryStatus,st)){state.theoryStatus=st;render()}if(st.length===members.length&&st.every(x=>x.ready))await transitionTheoryPlacement()}catch(e){console.warn(e)}}
 function startTheoryPoll(){if(theoryTimer)return;updateTheoryStatus();theoryTimer=setInterval(updateTheoryStatus,1900)}function stopTheoryPoll(){if(theoryTimer){clearInterval(theoryTimer);theoryTimer=null}}
 async function transitionTheoryPlacement(){if(state.phase!=='theory-select'||busy)return;const s=cloneState();s.phase='theory-place';s.theoryQueue=playerOrder(s);s.theoryPlaceIndex=0;s.current=s.theoryQueue[0];await commit(s,'가설 배치 시작')}
@@ -229,7 +229,7 @@
 async function placeTheory(autoKey=''){if(theoryPlacing||state.phase!=='theory-place'||state.theoryQueue[state.theoryPlaceIndex]!==mySeat())return;theoryPlacing=true;try{const round=Number(state.theoryRound||0);const rows=await rpc('boardmate_planetx_place_theory_choice',{p_token:token(),p_room_id:roomId,p_phase_id:round});const s=cloneState();for(const r of rows||[]){if(!s.theories.some(t=>t.id===r.id))s.theories.push({...r,submitRound:round,faceUp:false})}s.theoryPlaceIndex++;logPublic(s,`${memberName(mySeat())} · 가설 ${rows?.length||0}개 자동 배치`);if(s.theoryPlaceIndex<s.theoryQueue.length){s.current=s.theoryQueue[s.theoryPlaceIndex];const ok=await commit(s,'가설 자동 배치');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';return}const advanced=await rpc('boardmate_planetx_advance_theories',{p_token:token(),p_room_id:roomId,p_phase_id:s.theoryRound});const byId=Object.fromEntries((advanced||[]).map(x=>[x.id,x]));s.theories=s.theories.map(t=>byId[t.id]?{...t,depth:byId[t.id].depth}:t);const review=await rpc('boardmate_planetx_review_due',{p_token:token(),p_room_id:roomId});applyReview(s,review);await rotateToNextTurn(s);const ok=await commit(s,'가설 단계 완료');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey=''}catch(e){if(autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';toast((e.message||e)+' · 가설 배치를 자동 재시도합니다.','error');setTimeout(()=>{if(state?.phase==='theory-place'){theoryAutoKey='';maybeAutoPlaceTheory()}},1500)}finally{theoryPlacing=false}}
 function applyReview(s,r){const revealed=r?.revealed||[];for(const rv of revealed){const t=s.theories.find(x=>x.id===rv.id);if(t){Object.assign(t,{faceUp:true,object:rv.object,correct:rv.correct,removed:rv.correct===false})}}for(const c of r?.confirmed||[]){s.confirmed[c.sector]=c.object;logPublic(s,`교차 검증 · 섹터 ${c.sector} = ${objLabel(c.object)} 확인`)}for(const seat of r?.penalties||[]){const p=s.players[Number(seat)];if(p){p.time+=1;p.tie=++s.tieSeq;logPublic(s,`${memberName(Number(seat))} · 틀린 가설 시간 +1`)}}if(revealed.length&&!r?.confirmed?.length)logPublic(s,`교차 검증 · ${revealed.length}개 가설이 틀림으로 판정됨`)}
 
-function openFinalTheory(){const item=state.finalQueue[state.finalIndex],max=item.distance<=3?1:2;modal(`<h2>🏁 마지막 가설 제출</h2><p class="sub">최대 ${max}개. 이 최종 기회의 가설들은 채점상 동시에 제출된 것으로 처리됩니다.</p>${Array.from({length:max},(_,i)=>`<div class="form-row"><div class="field"><label>개체</label><select data-f-obj><option value="">제출 안 함</option>${optionObjects(false)}</select></div><div class="field"><label>섹터</label><select data-f-sec>${optionSectors()}</select></div></div>`).join('')}<div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn green" id="doFinalTheory">제출</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#doFinalTheory').onclick=async()=>{const os=[...m.querySelectorAll('[data-f-obj]')],ss=[...m.querySelectorAll('[data-f-sec]')],choices=os.map((o,i)=>o.value?{object:o.value,sector:Number(ss[i].value)}:null).filter(Boolean);try{const rows=await rpc('boardmate_planetx_final_submit',{p_token:token(),p_room_id:roomId,p_choices:choices,p_max:max});closeModal();const s=cloneState();for(const r of rows||[])s.theories.push({...r,submitRound:'최종',faceUp:false});logPublic(s,`${memberName(mySeat())} · 마지막 가설 ${rows?.length||0}개 제출`);advanceFinal(s);await commit(s,'마지막 가설')}catch(e){toast(e.message||e,'error')}}})}
+function openFinalTheory(){const item=state.finalQueue[state.finalIndex],max=item.distance<=3?1:2;modal(`<h2>🏁 마지막 가설 제출</h2><p class="sub">최대 ${max}개. 이 최종 기회의 가설들은 채점상 동시에 제출된 것으로 처리됩니다.</p>${Array.from({length:max},(_,i)=>`<div class="form-row"><div class="field"><label>개체</label><select data-f-obj><option value="">제출 안 함</option>${optionObjects(false)}</select></div><div class="field"><label>섹터</label><select data-f-sec>${optionSectors()}</select></div></div>`).join('')}<div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn green" id="doFinalTheory">제출</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#doFinalTheory').onclick=async()=>{const os=[...m.querySelectorAll('[data-f-obj]')],ss=[...m.querySelectorAll('[data-f-sec]')],choices=os.map((o,i)=>o.value?{object:o.value,sector:Number(ss[i].value)}:null).filter(Boolean);if(new Set(choices.map(x=>`${x.sector}:${x.object}`)).size!==choices.length){toast('같은 섹터에 같은 종류의 가설 토큰을 2개 제출할 수 없습니다.','error');return}try{const rows=await rpc('boardmate_planetx_final_submit',{p_token:token(),p_room_id:roomId,p_choices:choices,p_max:max});closeModal();const s=cloneState();for(const r of rows||[])s.theories.push({...r,submitRound:'최종',faceUp:false});logPublic(s,`${memberName(mySeat())} · 마지막 가설 ${rows?.length||0}개 제출`);advanceFinal(s);await commit(s,'마지막 가설')}catch(e){toast(e.message||e,'error')}}})}
 async function finishFinalLocate(correct){const s=cloneState(),item=s.finalQueue[s.finalIndex];if(correct)s.players[mySeat()].xScore=2*item.distance;logPublic(s,`${memberName(mySeat())} · 마지막 행성 X 찾기 ${correct?'성공':'실패'}`);advanceFinal(s);await commit(s,'마지막 행성 X 찾기')}
 function advanceFinal(s){s.finalIndex++;if(s.finalIndex>=s.finalQueue.length){s.phase='reveal';s.current=s.firstLocator?.seat??0}else{s.current=s.finalQueue[s.finalIndex].seat}}
 async function revealAndFinish(){if(busy||state.phase!=='reveal')return;try{busy=true;const data=await rpc('boardmate_planetx_final_data',{p_token:token(),p_room_id:roomId});const s=cloneState();s.board=data.board;const th=data.theories||[];const theoryPts={asteroid:2,comet:3,gas:4,dwarf:s.mode==='expert'?2:4};const score=members.map((_,seat)=>({seat,theory:0,leader:0,x:Number(s.players[seat].xScore||0),total:0,rank:0}));for(const t of th)if(t.correct)score[t.ownerSeat].theory+=(theoryPts[t.object]||0);for(let sec=1;sec<=sectorCount(s);sec++){const cs=th.filter(t=>t.sector===sec&&t.correct);if(!cs.length)continue;const min=Math.min(...cs.map(t=>Number(t.submitRound)));for(const seat of new Set(cs.filter(t=>Number(t.submitRound)===min).map(t=>t.ownerSeat)))score[seat].leader++}for(const x of score)x.total=x.theory+x.leader+x.x;score.sort((a,b)=>b.total-a.total||b.x-a.x||b.leader-a.leader||a.seat-b.seat);let rank=1;for(let i=0;i<score.length;i++){if(i&&!(score[i].total===score[i-1].total&&score[i].x===score[i-1].x&&score[i].leader===score[i-1].leader))rank=i+1;score[i].rank=rank}s.sharedTie=score.filter(x=>x.rank===1).length>1;s.scores=score;s.phase='finished';s.over=true;s.current=s.firstLocator?.seat??0;s.revealedTheories=th;logPublic(s,'모든 섹터와 남은 가설 공개 · 최종 점수 계산 완료');busy=false;await commit(s,'최종 점수')}catch(e){busy=false;toast(e.message||e,'error')}}
--- a/index.html
+++ b/index.html
@@ -13,6 +13,6 @@
   <div id="app"></div>
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   <script src="./config.js"></script>
-  <script type="module" src="./app.js?v=42"></script>
+  <script type="module" src="./app.js?v=43"></script>
 </body>
 </html>
--- a/sw.js
+++ b/sw.js
@@ -1,4 +1,4 @@
-const CACHE='boardmate-shell-v11.4.42';
+const CACHE='boardmate-shell-v11.4.43';
 self.addEventListener('install',()=>self.skipWaiting());
 self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('boardmate-shell-')&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
 self.addEventListener('fetch',event=>{
--- a/SUPABASE_PLANETX_PVP_V1.sql
+++ b/SUPABASE_PLANETX_PVP_V1.sql
@@ -816,3 +816,189 @@
 commit;
 
 -- After this script finishes, PostgREST may need a few seconds to refresh its schema cache.
+
+
+-- v11.4.43: theory same-sector rule fix (different object types may share a sector)
+-- BoardMate Arena v11.4.43
+-- Planet X: allow two DIFFERENT theory tokens on the same sector.
+-- Rule: same sector + same object type cannot be submitted twice by the same player.
+-- Run this file once in Supabase SQL Editor for an existing deployment.
+
+create or replace function public.boardmate_planetx_set_theory_choice(
+  p_token text,
+  p_room_id uuid,
+  p_phase_id integer,
+  p_choices jsonb
+)
+returns jsonb
+language plpgsql
+security definer
+set search_path=public,extensions
+as $$
+declare
+  uid uuid;
+  seat_no integer;
+  sess public.boardmate_planetx_sessions%rowtype;
+  st jsonb;
+  maxn integer;
+  x jsonb;
+  sec integer;
+  obj text;
+  used integer;
+  pending_same_type integer;
+  choice_n integer;
+begin
+  uid:=public.boardmate_session_user(p_token);
+  seat_no:=public.boardmate_planetx_seat(p_token,p_room_id);
+  if uid is null or seat_no is null then raise exception '참가자가 아닙니다.'; end if;
+
+  select state into st from public.boardmate_room_state where room_id=p_room_id;
+  if st is null or st->>'phase'<>'theory-select'
+     or coalesce(nullif(st->>'theoryRound','')::integer,-1)<>p_phase_id then
+    raise exception '현재 가설 선택 단계가 아닙니다.';
+  end if;
+
+  select * into sess from public.boardmate_planetx_sessions where room_id=p_room_id;
+  maxn:=case when sess.mode='expert' then 2 else 1 end;
+
+  if jsonb_typeof(coalesce(p_choices,'[]'::jsonb))<>'array' then
+    raise exception '가설 형식을 확인해 주세요.';
+  end if;
+  choice_n:=jsonb_array_length(coalesce(p_choices,'[]'::jsonb));
+  if choice_n>maxn then raise exception '이번 가설 단계의 제출 수를 확인해 주세요.'; end if;
+
+  -- Different object types MAY share a sector. Only an identical sector+object pair is forbidden.
+  if exists (
+    select 1
+    from jsonb_array_elements(coalesce(p_choices,'[]'::jsonb)) z(value)
+    group by value->>'sector', value->>'object'
+    having count(*) > 1
+  ) then
+    raise exception '같은 섹터에 같은 종류의 가설 토큰을 2개 제출할 수 없습니다.';
+  end if;
+
+  for x in select value from jsonb_array_elements(coalesce(p_choices,'[]'::jsonb)) loop
+    sec:=nullif(x->>'sector','')::integer;
+    obj:=x->>'object';
+    if sec is null or sec<1 or sec>(case when sess.mode='expert' then 18 else 12 end)
+       or obj not in ('asteroid','comet','dwarf','gas') then
+      raise exception '가설 내용을 확인해 주세요.';
+    end if;
+    if coalesce(st->'confirmed','{}'::jsonb)?sec::text then
+      raise exception '이미 교차 검증으로 확인된 섹터에는 가설을 낼 수 없습니다.';
+    end if;
+    if exists(
+      select 1 from public.boardmate_planetx_theories
+      where room_id=p_room_id and owner_uid=uid and sector=sec and object_type=obj
+    ) then
+      raise exception '같은 섹터에 같은 종류의 가설을 다시 제출할 수 없습니다.';
+    end if;
+
+    select count(*) into used
+    from public.boardmate_planetx_theories
+    where room_id=p_room_id and owner_uid=uid and object_type=obj;
+
+    select count(*) into pending_same_type
+    from jsonb_array_elements(coalesce(p_choices,'[]'::jsonb)) z(value)
+    where value->>'object'=obj;
+
+    if used + pending_same_type > public.boardmate_planetx_token_limit(sess.mode,obj) then
+      raise exception '해당 종류의 가설 토큰을 모두 사용했습니다.';
+    end if;
+  end loop;
+
+  insert into public.boardmate_planetx_choices(room_id,user_id,seat,phase_id,choices,ready,placed,updated_at)
+  values(p_room_id,uid,seat_no,p_phase_id,coalesce(p_choices,'[]'::jsonb),true,false,now())
+  on conflict(room_id,user_id,phase_id)
+  do update set choices=excluded.choices,ready=true,placed=false,updated_at=now();
+
+  return jsonb_build_object('ready',true,'count',choice_n);
+end;
+$$;
+
+grant execute on function public.boardmate_planetx_set_theory_choice(text,uuid,integer,jsonb) to anon,authenticated;
+
+create or replace function public.boardmate_planetx_final_submit(
+  p_token text,
+  p_room_id uuid,
+  p_choices jsonb,
+  p_max integer
+)
+returns jsonb
+language plpgsql
+security definer
+set search_path=public,extensions
+as $$
+declare
+  uid uuid;
+  seat_no integer;
+  sess public.boardmate_planetx_sessions%rowtype;
+  st jsonb;
+  x jsonb;
+  outj jsonb:='[]'::jsonb;
+  tid uuid;
+  obj text;
+  sec integer;
+  used integer;
+  choice_n integer;
+begin
+  uid:=public.boardmate_session_user(p_token);
+  seat_no:=public.boardmate_planetx_assert_action(p_token,p_room_id,array['final'],true);
+  select state into st from public.boardmate_room_state where room_id=p_room_id;
+  select * into sess from public.boardmate_planetx_sessions where room_id=p_room_id;
+
+  if p_max not in(1,2) or jsonb_typeof(coalesce(p_choices,'[]'::jsonb))<>'array' then
+    raise exception '제출 가능한 가설 수를 확인해 주세요.';
+  end if;
+  choice_n:=jsonb_array_length(coalesce(p_choices,'[]'::jsonb));
+  if choice_n>p_max then raise exception '제출 가능한 가설 수를 확인해 주세요.'; end if;
+
+  -- Same sector is allowed when the object types differ.
+  if exists (
+    select 1
+    from jsonb_array_elements(coalesce(p_choices,'[]'::jsonb)) z(value)
+    group by value->>'sector', value->>'object'
+    having count(*) > 1
+  ) then
+    raise exception '같은 섹터에 같은 종류의 가설 토큰을 2개 제출할 수 없습니다.';
+  end if;
+
+  for x in select value from jsonb_array_elements(coalesce(p_choices,'[]'::jsonb)) loop
+    obj:=x->>'object';
+    sec:=nullif(x->>'sector','')::integer;
+    if obj not in('asteroid','comet','dwarf','gas')
+       or sec is null or sec<1 or sec>(case when sess.mode='expert' then 18 else 12 end) then
+      raise exception '가설 내용을 확인해 주세요.';
+    end if;
+    if coalesce(st->'confirmed','{}'::jsonb)?sec::text then
+      raise exception '이미 확인된 섹터에는 가설을 낼 수 없습니다.';
+    end if;
+    if exists(
+      select 1 from public.boardmate_planetx_theories
+      where room_id=p_room_id and owner_uid=uid and sector=sec and object_type=obj
+    ) then
+      raise exception '같은 섹터에 같은 가설을 다시 낼 수 없습니다.';
+    end if;
+
+    select count(*) into used
+    from public.boardmate_planetx_theories
+    where room_id=p_room_id and owner_uid=uid and object_type=obj;
+    if used>=public.boardmate_planetx_token_limit(sess.mode,obj) then
+      raise exception '해당 종류의 가설 토큰을 모두 사용했습니다.';
+    end if;
+
+    insert into public.boardmate_planetx_theories(
+      room_id,owner_uid,owner_seat,object_type,sector,depth,submit_round,final
+    ) values(
+      p_room_id,uid,seat_no,obj,sec,0,1000000,true
+    ) returning id into tid;
+
+    outj:=outj||jsonb_build_array(jsonb_build_object(
+      'id',tid,'ownerSeat',seat_no,'sector',sec,'depth',0,'final',true
+    ));
+  end loop;
+  return outj;
+end;
+$$;
+
+grant execute on function public.boardmate_planetx_final_submit(text,uuid,jsonb,integer) to anon,authenticated;
--- a/HANDOFF_VERSION.txt
+++ b/HANDOFF_VERSION.txt
@@ -1 +1 @@
-v11.4.41
+v11.4.43
