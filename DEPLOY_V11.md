# BoardMate Arcade v11 배포 순서

1. GitHub Pages 저장소 root에 v11 파일을 덮어씁니다.
2. **기존에 실제 Supabase URL/key가 들어 있는 `config.js`는 반드시 기존 값을 유지**합니다. ZIP의 빈 예시값으로 덮어쓰지 마세요.
3. Supabase > SQL Editor에서 다음 중 하나를 실행합니다.
   - 권장: `supabase.sql` 전체 실행
   - 취소 오류만 빠르게 고칠 때: `SUPABASE_CANCEL_FIX.sql` 전체 실행
4. 새 캘리코/캐스캐디아는 상태 구조가 바뀌었으므로 기존 진행 중 방이 아니라 **새 방을 만들어 테스트**합니다.
5. 2개 브라우저/2개 계정으로 방 생성 → 참가 → 시작 → 한 턴 → 새로고침/재접속 → 취소 투표를 확인합니다.

## v11 확인 포인트
- 취소 버튼: `N / 전체명` 표시, 전원 동의 시 `cancelled`, ELO 미반영
- 캘리코: 새 Arena HTML UI, 호스트 설정, 각자 차례 제한, 자동 저장/재접속, 현재 차례 표시
- 캐스캐디아: A/가족/중급 점수 선택, 타일 회전 시 모든 후보 위치 34% 미리보기, 자동 저장/재접속
- 펜토리니: 선택한 현재 방향의 펜토미노가 합법 위치 전체에 34%로 표시, 회전/뒤집기 즉시 반영
- 포크노바: 화면 표시 이름만 변경, 내부 `pocketnova` 키와 파일 경로는 호환을 위해 유지
