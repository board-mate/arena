# BoardMate Arcade v11.2 배포

이번 변경은 **온라인 캘리코 디자인 타일 선택 흐름만 수정**합니다.
DB 스키마/RPC 변경은 없습니다.

## 변경 내용
- 방장: 게임 설정 + 각 좌석의 디자인 후보 4개 생성
- 각 참가자: 자기 좌석 후보 4개 중 3개만 직접 선택
- 다른 참가자의 후보는 클릭 불가, 선택 진행/완료 상태만 표시
- 선택 순서 = 위쪽 → 왼쪽 아래 → 오른쪽
- 전원 선택 완료 후 방장만 게임 시작 가능
- 디자인 선택 상태도 Supabase 방 상태에 자동 저장/재접속
- 동시 선택 revision conflict 시 자기 좌석 선택만 최신 상태에 병합 후 재시도

## 배포 순서
1. 현재 방 생성/게임 취소가 정상이라면 **Supabase SQL은 실행하지 않습니다.**
2. GitHub Pages 파일에서 `online-calico.html`을 v11.2 파일로 교체합니다.
   - 전체 ZIP을 덮어써도 됩니다.
   - `config.js` 운영값은 기존 값을 유지하세요.
3. GitHub Pages 반영 후 브라우저에서 강력 새로고침합니다.
4. 새 캘리코 방을 만들어 2계정으로 테스트합니다.
   - 방장이 디자인 선택 단계 시작
   - 1번 계정은 1번 좌석 후보만 선택 가능한지 확인
   - 2번 계정은 2번 좌석 후보만 선택 가능한지 확인
   - 상대 좌석은 `선택 중/선택 완료`만 보이는지 확인
   - 둘 다 3개 선택 전에는 방장 시작 버튼 비활성
   - 둘 다 3개 선택 후 방장 시작 버튼 활성
   - 디자인 선택 도중 새로고침 후 선택 상태 복구 확인

## SQL
- v11.2 추가 SQL 없음
- `supabase.sql`은 계속 전체 기준본으로 유지
- `SUPABASE_RPC_FIX_v11_1.sql`은 방 생성/취소 RPC 복구가 다시 필요할 때만 사용
