# Calico V5 — Printed Edge Tile Automation

## 변경 사항
- 파랑 / 초록 / 보라 / 노랑 4개 플레이어 보드의 인쇄 가장자리 타일을 22개 가상 헥스 위치로 모델링.
- 각 보드의 실제 원본 보드 이미지와 원본 타일 아트(6색 × 6무늬)를 브라우저에서 대조해 색/무늬 데이터를 자동 추출.
- 추출 데이터는 플레이어별 `edgeCatalog`에 저장하고 localStorage에도 캐시.
- 같은 색 연결 그룹(`button`)과 같은 무늬 연결 그룹(`cat`)을 내부 25칸 + 인쇄 가장자리 전체를 합친 그래프로 판정.
- 인쇄 타일은 영구 타일이므로 토큰을 붙일 수 있는 일반 플레이어 칸으로 취급하지 않고, 그룹 연결용 노드로만 사용.
- 브라우저에서 가장자리 이미지/타일 아트를 불러오지 못하거나 분류 확신도가 부족하면 타일 배치를 막아 잘못된 토큰 지급을 방지하는 fail-closed 방식.

## 가장자리 좌표
총 22개:
- 상단: `-1,0` ~ `-1,5` 6개
- 좌우: `0,-1` ~ `4,-1`, `0,5` ~ `4,5` 각 5개씩 = 10개
- 하단: `5,0` ~ `5,5` 6개

합계 22개 × 4개 보드 = 88개 인쇄 타일 위치.

## 자동 판정
- 단추: 동일 색 연결 컴포넌트가 3개 이상이면 해당 컴포넌트에서 최초 1개만 지급.
- 고양이: 선호 무늬의 연결 컴포넌트가 실제 보드 경계를 넘어가는 경우에도 포함하며, 크기/도형 조건을 동일하게 검사.
- 기존 토큰이 포함된 컴포넌트는 동일 그룹의 중복 토큰 지급을 차단.
- 무지개 단추 조건은 기존 6색 단추 보유 여부를 유지.

## 중요
이번 V5는 가장자리 타일을 별도 하드코딩된 추측값으로 넣는 방식이 아니라, 현재 게임이 사용하는 원본 Calico 보드 이미지와 타일 아트를 이용해 실행 시 실제 색/무늬 데이터를 추출한다. 분류가 확실하지 않으면 진행을 중단하여 잘못된 자동 지급을 방지한다.
