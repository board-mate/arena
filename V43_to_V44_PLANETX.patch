--- /mnt/data/arena_work/online-planetx.html	2026-09-09 14:36:37.023942211 +0000
+++ /mnt/data/arena_v44_work/online-planetx.html	2026-09-09 14:42:39.071160492 +0000
@@ -4,7 +4,7 @@
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
 <title>BoardMate Online · 행성 X를 찾아서</title>
-<!-- BoardMate Arena v11.4.43 · Planet X theory same-sector rule fix -->
+<!-- BoardMate Arena v11.4.44 · Planet X account private sheet + theory placement rollback -->
 <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
 <script src="./config.js"></script>
 <style>
@@ -79,6 +79,8 @@
 function publicLogHtml(raw){const t=displayKo(raw);for(let i=0;i<members.length;i++){const n=memberName(i),prefix=n+' ·';if(t.startsWith(prefix))return `${playerNameHtml(i)}${esc(t.slice(n.length))}`;}return esc(t)}
 const PRIVATE_KEY=()=>`boardmate:planetx:private:${roomId}:${me?.user_id||'anon'}`;
 let room=null,members=[],me=null,state=null,revision=-1,busy=false,saving=false,setupTimer=null,theoryTimer=null,selectedMode='standard',selectedDiff='beginner';
+let theoryPlacing=false,theoryAutoKey='';
+let privateSaveChain=Promise.resolve();
 let uiRecordOpen=false;
 let researchTopics={};
 let priv={hints:[],season:'',difficulty:'',observations:[],marks:{},research:{}};
@@ -94,8 +96,9 @@
 function activeSeat(s=state){return playerOrder(s)[0]??0}
 function canAct(){return state?.phase==='turn'&&state.current===mySeat()&&!busy}
 function objLabel(x){return OBJ[x]||x}
-function savePrivate(){localStorage.setItem(PRIVATE_KEY(),JSON.stringify(priv))}
-function loadPrivate(){try{priv=Object.assign(priv,JSON.parse(localStorage.getItem(PRIVATE_KEY())||'{}'))}catch{};priv.hints??=[];priv.observations??=[];priv.marks??={};priv.research??={}}
+function normalizePrivate(v={}){const out=Object.assign({hints:[],season:'',difficulty:'',observations:[],marks:{},research:{}},v||{});out.hints??=[];out.observations??=[];out.marks??={};out.research??={};return out}
+function savePrivate(){const snapshot=structuredClone(normalizePrivate(priv));privateSaveChain=privateSaveChain.catch(()=>{}).then(()=>rpc('boardmate_planetx_save_private_sheet',{p_token:token(),p_room_id:roomId,p_data:snapshot})).catch(e=>{console.warn('[planetx private save]',e);toast('내 기록지 계정 저장에 실패했습니다. 잠시 후 다시 시도해 주세요.','error')});return privateSaveChain}
+async function loadPrivate(){let server=null;try{server=await rpc('boardmate_planetx_get_private_sheet',{p_token:token(),p_room_id:roomId})}catch(e){console.warn('[planetx private load]',e);throw e}const legacyKey=PRIVATE_KEY();let legacy=null;try{legacy=JSON.parse(localStorage.getItem(legacyKey)||'null')}catch{};const hasServer=server&&typeof server==='object'&&Object.keys(server).length>0;const hasLegacy=legacy&&typeof legacy==='object'&&Object.keys(legacy).length>0;if(!hasServer&&hasLegacy){priv=normalizePrivate(legacy);await rpc('boardmate_planetx_save_private_sheet',{p_token:token(),p_room_id:roomId,p_data:structuredClone(priv)});try{localStorage.removeItem(legacyKey)}catch{};toast('기존 브라우저 기록을 계정 기록지로 옮겼습니다.')}else{priv=normalizePrivate(hasServer?server:{});if(hasServer){try{localStorage.removeItem(legacyKey)}catch{}}}}
 function toast(msg,kind=''){let d=document.createElement('div');d.textContent=msg;d.style.cssText=`position:fixed;z-index:3000;left:50%;bottom:22px;transform:translateX(-50%);background:${kind==='error'?'#722635':'#18334c'};color:white;border:1px solid #55738e;border-radius:12px;padding:10px 14px;box-shadow:0 10px 30px #0008;font-weight:800;font-size:12px`;document.body.appendChild(d);setTimeout(()=>d.remove(),2600)}
 function modal(html,bind){const r=document.querySelector('#modalRoot');r.innerHTML=`<div class="modal-back"><div class="modal">${html}</div></div>`;const m=r.firstElementChild.firstElementChild;m.parentElement.addEventListener('click',e=>{if(e.target===m.parentElement)closeModal()});bind?.(m)}
 function closeModal(){document.querySelector('#modalRoot').innerHTML=''}
@@ -125,7 +128,7 @@
     s.theoryRound=Number(s.theoryRound||0)+1;s.phase='theory-select';s.theoryPlaceIndex=0;s.theoryQueue=[];s.current=activeSeat(s);logPublic(s,`가설 단계 ${s.theoryRound} 시작`);return s;
   }
 }
-function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.43',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
+function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.44',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
 
 function captureScroll(){const inner={};document.querySelectorAll('[data-scroll-key]').forEach(el=>{inner[el.dataset.scrollKey]={top:el.scrollTop,left:el.scrollLeft}});return{x:window.scrollX,y:window.scrollY,inner}}
 function restoreScroll(snap){if(!snap)return;requestAnimationFrame(()=>{window.scrollTo(snap.x,snap.y);document.querySelectorAll('[data-scroll-key]').forEach(el=>{const p=snap.inner?.[el.dataset.scrollKey];if(p){el.scrollTop=p.top;el.scrollLeft=p.left}})})}
@@ -186,7 +189,7 @@
  const sectors=Array.from({length:n},(_,i)=>i+1).map(sec=>{const cf=state.confirmed?.[sec],ts=(state.theories||[]).filter(t=>t.sector===sec&&!t.removed);return`<div class="sector ${vis.has(sec)?'visible':''} ${cf?'confirmed':''}"><div style="display:flex;justify-content:space-between;align-items:flex-start"><div><span class="no">${sec}</span><span class="opposite-tag">↔ 맞은편 ${oppositeSector(sec)}</span></div>${vis.has(sec)?'<span class="vis">관찰 가능</span>':''}</div>${cf?`<div class="confirmed-label">${objLabel(cf)}</div>`:'<div class="sub" style="margin-top:8px">미확인</div>'}<div class="track">${ts.map(t=>`<div class="theory ${t.faceUp?'face':''} ${t.correct===false?'wrong':''}" style="border-color:${playerColor(t.ownerSeat)};box-shadow:inset 0 0 0 1px ${playerColor(t.ownerSeat)}55" title="${esc(memberName(t.ownerSeat))} (${playerColorName(t.ownerSeat)}) · 깊이 ${t.depth}">${t.faceUp?OBJICON[t.object]||'?':'?'}<small class="theory-owner" style="background:${playerColor(t.ownerSeat)};color:#07111f">${t.ownerSeat+1}</small></div>`).join('')}</div><div class="depth">${ts.length?`가설 · ${ts.map(t=>`${t.ownerSeat+1}번 제출자 · ${t.submitRound?`${t.submitRound}차 · `:''}${Math.max(1,Number(t.depth||1))}/3`).join(' / ')}`:'가설 없음'}</div></div>`}).join('');
  const ticks=Array.from({length:n},(_,i)=>i+1).map(sec=>{const here=state.players.filter(p=>((p.time-1)%n)+1===sec);return`<div class="tick ${((state.earth-1)%n)+1===sec?'earth':''} ${isTheorySector(state.mode,sec)?'theory-event':''} ${conferenceAt(state.mode,sec)?'conf-event':''}"><span class="tick-num">${sec}</span><span class="tick-pawns">${here.map(p=>`<i class="pawn-dot ${p.seat===current?'active':''}" style="background:${playerColor(p.seat)}" title="${esc(memberName(p.seat))} · 시간 ${p.time}"></i>`).join('')}</span></div>`}).join('');
  const conf=Object.entries(state.conferences||{}).map(([k,v])=>`<div class="conference"><b>${k} 회의 · ${esc(topicKo(v.topic||''))}</b><div class="sub" style="color:#f3e7bd">${esc(ruleKo(v.rule||''))}</div></div>`).join('');
- document.querySelector('#app').innerHTML=`<div class="players" data-scroll-key="players">${playerCards}</div><div class="grid"><section class="card board-card"><div class="board-head"><div><h2>태양계 게임판 · ${state.mode==='expert'?'전문가':'표준'} 모드</h2><div class="sub">게임 코드 ${esc(state.code)} · 지구 위치 ${((state.earth-1)%n)+1} · 관찰 가능 하늘 ${visibleSectors().join(', ')}</div></div><span class="badge gold">${eventNote}</span></div>${orbitPanel()}<div class="time-line" data-scroll-key="time-line">${ticks}</div><div class="sub board-legend">📄 가설 단계 · 🔭 회의 · 원형 보드의 색 점은 플레이어 시간 위치입니다.</div>${privateObservationPanel()}<details class="sector-detail-box"><summary>🧩 섹터 상세 · 가설 트랙</summary><div class="sky ${state.mode==='expert'?'expert':''}">${sectors}</div></details></section><aside class="game-side"><section class="card"><h3>현재 진행</h3>${phasePanel()}</section><section class="card"><h3>참조표</h3>${referencePanel()}</section><section class="card"><h3>공개 회의 정보</h3>${conf||'<div class="sub">아직 공개된 회의 정보가 없습니다.</div>'}</section><section class="card"><h3>공개 기록</h3><div class="log" data-scroll-key="public-log">${(state.log||[]).map(x=>`<div class="line"><b>${esc(x.at)}</b> · ${publicLogHtml(x.text)}</div>`).join('')||'<div class="sub">기록 없음</div>'}</div></section><details class="card private private-sheet private-sheet-box" ${uiRecordOpen?'open':''}><summary>🔒 내 기록지</summary><div class="private-sheet-body"><div class="sub">시작 정보와 섹터 메모는 이 브라우저에만 저장됩니다.</div><div class="sidebar-priority-note">참조표 · 연구 주제 · 공개 기록을 먼저 볼 수 있도록 기록지는 맨 아래에 두었습니다.</div><div class="abbr-legend">약자: A=소행성 · C=혜성 · D=왜소행성 · G=가스 구름 · E=비어 보임 · X=행성 X</div>${privatePanel()}</div></details></aside>${state.phase==='finished'?`<section class="card full">${scorePanel()}</section>`:''}</div>`;
+ document.querySelector('#app').innerHTML=`<div class="players" data-scroll-key="players">${playerCards}</div><div class="grid"><section class="card board-card"><div class="board-head"><div><h2>태양계 게임판 · ${state.mode==='expert'?'전문가':'표준'} 모드</h2><div class="sub">게임 코드 ${esc(state.code)} · 지구 위치 ${((state.earth-1)%n)+1} · 관찰 가능 하늘 ${visibleSectors().join(', ')}</div></div><span class="badge gold">${eventNote}</span></div>${orbitPanel()}<div class="time-line" data-scroll-key="time-line">${ticks}</div><div class="sub board-legend">📄 가설 단계 · 🔭 회의 · 원형 보드의 색 점은 플레이어 시간 위치입니다.</div>${privateObservationPanel()}<details class="sector-detail-box"><summary>🧩 섹터 상세 · 가설 트랙</summary><div class="sky ${state.mode==='expert'?'expert':''}">${sectors}</div></details></section><aside class="game-side"><section class="card"><h3>현재 진행</h3>${phasePanel()}</section><section class="card"><h3>참조표</h3>${referencePanel()}</section><section class="card"><h3>공개 회의 정보</h3>${conf||'<div class="sub">아직 공개된 회의 정보가 없습니다.</div>'}</section><section class="card"><h3>공개 기록</h3><div class="log" data-scroll-key="public-log">${(state.log||[]).map(x=>`<div class="line"><b>${esc(x.at)}</b> · ${publicLogHtml(x.text)}</div>`).join('')||'<div class="sub">기록 없음</div>'}</div></section><details class="card private private-sheet private-sheet-box" ${uiRecordOpen?'open':''}><summary>🔒 내 기록지</summary><div class="private-sheet-body"><div class="sub">시작 정보·조사 기록·섹터 메모는 로그인 계정에 비공개로 저장됩니다.</div><div class="sidebar-priority-note">참조표 · 연구 주제 · 공개 기록을 먼저 볼 수 있도록 기록지는 맨 아래에 두었습니다.</div><div class="abbr-legend">약자: A=소행성 · C=혜성 · D=왜소행성 · G=가스 구름 · E=비어 보임 · X=행성 X</div>${privatePanel()}</div></details></aside>${state.phase==='finished'?`<section class="card full">${scorePanel()}</section>`:''}</div>`;
  bindGameControls();
  maybeAutoPlaceTheory();
 }
@@ -194,7 +197,7 @@
  const seat=mySeat(),p=state.players[seat];
  if(state.phase==='turn')return state.current===seat?`<div class="notice"><b>당신의 차례입니다.</b><br>액션 1개를 선택하세요. 액션 후 시간 비용만큼 자동 이동하고 지구가 회전합니다.</div><div class="actions"><button class="btn primary" id="survey">🔭 개체 탐사</button><button class="btn primary" id="target" ${p.targetUsed>=2?'disabled':''}>🎯 섹터 지정</button><button class="btn primary" id="research" ${p.lastAction==='research'?'disabled':''}>📚 주제 조사</button><button class="btn gold" id="locate">✦ 행성 X 찾기</button></div>`:`<div class="notice"><b>${playerNameHtml(state.current)}</b>의 차례입니다.<br>상대가 비공개 결과를 확인하고 행동을 마칠 때까지 기다리세요.</div>`;
  if(state.phase==='theory-select')return theorySelectPanel();
- if(state.phase==='theory-place'){const q=state.theoryQueue||[],who=q[state.theoryPlaceIndex];return who===seat?`<div class="notice auto-place-note"><span>선택한 가설을 보드에 자동 배치하고 있습니다. 잠시 멈추면 아래 버튼으로 다시 시도할 수 있습니다.</span></div><button id="retryTheoryPlace" class="btn primary">가설 배치 계속</button>`:`<div class="notice"><b>${playerNameHtml(who)}</b>의 가설을 자동 배치하는 중입니다.</div>`}
+ if(state.phase==='theory-place'){const q=state.theoryQueue||[],who=q[state.theoryPlaceIndex];return who===seat?`<div class="notice auto-place-note"><span>선택한 가설을 보드에 자동 배치하고 있습니다. 별도 확정은 필요 없습니다.</span></div>`:`<div class="notice"><b>${playerNameHtml(who)}</b>의 가설을 자동 배치하는 중입니다.</div>`}
  if(state.phase==='final')return finalPanel();
  if(state.phase==='reveal')return`<div class="notice">최종 정답과 모든 남은 가설을 공개하고 점수를 계산하는 중입니다.</div>`;
  if(state.phase==='finished')return`<div class="notice">게임이 종료되었습니다. 아래 최종 점수를 확인하세요.</div>`;
@@ -203,12 +206,12 @@
 function theorySelectPanel(){const status=state.theoryStatus||[],mine=status.find(x=>x.seat===mySeat());return`<div class="notice">모든 플레이어가 동시에 가설을 비공개로 선택합니다. ${state.mode==='expert'?'최대 2개':'최대 1개'}이며 0개 제출도 가능합니다.</div><button id="chooseTheory" class="btn ${mine?.ready?'ghost':'green'}">${mine?.ready?'내 선택 수정':'가설 비공개 선택'}</button><div class="theory-status">${members.map((m,i)=>{const x=status.find(a=>a.seat===i);return`<span class="badge ${x?.ready?'gold':''}" style="border:1px solid ${playerColor(i)}66"><span style="color:${playerColor(i)}">●</span> ${x?.ready?'✓':'…'} ${esc(memberName(i))}</span>`}).join('')}</div>`}
 function finalPanel(){const item=state.finalQueue?.[state.finalIndex];if(!item)return`<button id="finishReveal" class="btn gold">정답 공개 · 점수 계산</button>`;if(item.seat!==mySeat())return`<div class="notice"><b>${playerNameHtml(item.seat)}</b>의 마지막 점수 기회입니다.<br>선두보다 ${item.distance}칸 뒤였습니다.</div>`;const max=item.distance<=3?1:2;return`<div class="notice"><b>마지막 점수 기회</b><br>선두보다 ${item.distance}칸 뒤 · 가설 ${max}개 제출 <b>또는</b> 행성 X 찾기를 시도할 수 있습니다. 이때 시간은 이동하지 않습니다.</div><div class="actions"><button id="finalTheory" class="btn green">가설 최대 ${max}개 제출</button><button id="finalLocate" class="btn gold">행성 X 찾기</button></div>`}
 function privateObservationPanel(){const rows=priv.observations||[];return`<section class="private-quick"><div class="private-quick-head"><h3>🔒 내 비공개 조사 기록</h3><span class="badge">${rows.length}건</span></div><div class="record-tip">조사 결과가 최신순으로 바로 쌓입니다. 보드 아래에서 계속 확인할 수 있습니다.</div><div class="obs quick-obs" data-scroll-key="private-observations" style="margin-top:7px">${rows.map(o=>`<div class="line"><b>${esc(o.at)} · ${esc(o.type)}</b><br>${esc(displayKo(o.text))}</div>`).join('')||'<div class="sub">아직 조사 기록이 없습니다.</div>'}</div></section>`}
-function privatePanel(){const allowedComets=new Set(cometSectors());const marks=Array.from({length:sectorCount()},(_,i)=>i+1).map(sec=>{const objects=['asteroid',...(allowedComets.has(sec)?['comet']:[]),'dwarf','gas','empty','x'];return`<div class="note-cell ${allowedComets.has(sec)?'comet-possible':''}"><b>${sec}</b>${allowedComets.has(sec)?'<span class="comet-possible-label">🌠 혜성 가능</span>':''}<div class="mark-row">${objects.map(o=>{const v=priv.marks?.[sec]?.[o]||'';return`<button class="mark ${v}" title="${esc(objLabel(o))}" data-mark="${sec}:${o}">${OBJNOTE[o]}<span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}</div></div>`}).join('');return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">시작 정보가 이 기기에 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><h3 style="margin-top:14px">📝 섹터 메모</h3><div class="sub" style="margin-bottom:7px">미정 → × 없음 → ✓ 있음. 혜성은 가능한 섹터에만 버튼이 표시됩니다.</div><div class="note-grid">${marks}</div>`}
+function privatePanel(){const allowedComets=new Set(cometSectors());const marks=Array.from({length:sectorCount()},(_,i)=>i+1).map(sec=>{const objects=['asteroid',...(allowedComets.has(sec)?['comet']:[]),'dwarf','gas','empty','x'];return`<div class="note-cell ${allowedComets.has(sec)?'comet-possible':''}"><b>${sec}</b>${allowedComets.has(sec)?'<span class="comet-possible-label">🌠 혜성 가능</span>':''}<div class="mark-row">${objects.map(o=>{const v=priv.marks?.[sec]?.[o]||'';return`<button class="mark ${v}" title="${esc(objLabel(o))}" data-mark="${sec}:${o}">${OBJNOTE[o]}<span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}</div></div>`}).join('');return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">계정에 저장된 시작 정보가 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><h3 style="margin-top:14px">📝 섹터 메모</h3><div class="sub" style="margin-bottom:7px">미정 → × 없음 → ✓ 있음. 혜성은 가능한 섹터에만 버튼이 표시됩니다.</div><div class="note-grid">${marks}</div>`}
 
 function scorePanel(){const scores=state.scores||[];return`<h2>🏆 최종 점수</h2><table class="score-table"><thead><tr><th>플레이어</th><th>가설</th><th>리더</th><th>행성 X</th><th>합계</th></tr></thead><tbody>${scores.map(x=>`<tr><td style="border-left:4px solid ${playerColor(x.seat)}">${x.rank===1?'🏆 ':''}<span style="color:${playerColor(x.seat)}">●</span> ${esc(memberName(x.seat))} <span class="player-color-name" style="--pc:${playerColor(x.seat)}">${playerColorName(x.seat)}</span></td><td>${x.theory}</td><td>${x.leader}</td><td>${x.x}</td><td><b>${x.total}</b></td></tr>`).join('')}</tbody></table>${state.sharedTie?'<div class="notice" style="margin-top:10px">공식 타이브레이커 후에도 완전 동점이라 공동 승리입니다. 이 경우 BoardMate ELO는 자동 반영하지 않습니다.</div>':''}${endPanel('게임 종료',scores.filter(x=>x.rank===1).map(x=>memberName(x.seat)).join(', ')+' 승리')}`}
 
 function bindGameControls(){
- document.querySelector('#survey')?.addEventListener('click',openSurvey);document.querySelector('#target')?.addEventListener('click',openTarget);document.querySelector('#research')?.addEventListener('click',openResearch);document.querySelector('#locate')?.addEventListener('click',()=>openLocate(false));document.querySelector('#chooseTheory')?.addEventListener('click',openTheoryChoice);document.querySelector('#retryTheoryPlace')?.addEventListener('click',()=>{theoryAutoKey='';maybeAutoPlaceTheory()});document.querySelector('#finalTheory')?.addEventListener('click',openFinalTheory);document.querySelector('#finalLocate')?.addEventListener('click',()=>openLocate(true));document.querySelector('#finishReveal')?.addEventListener('click',async()=>{const s=cloneState();s.phase='reveal';await commit(s,'정답 공개')});
+ document.querySelector('#survey')?.addEventListener('click',openSurvey);document.querySelector('#target')?.addEventListener('click',openTarget);document.querySelector('#research')?.addEventListener('click',openResearch);document.querySelector('#locate')?.addEventListener('click',()=>openLocate(false));document.querySelector('#chooseTheory')?.addEventListener('click',openTheoryChoice);document.querySelector('#finalTheory')?.addEventListener('click',openFinalTheory);document.querySelector('#finalLocate')?.addEventListener('click',()=>openLocate(true));document.querySelector('#finishReveal')?.addEventListener('click',async()=>{const s=cloneState();s.phase='reveal';await commit(s,'정답 공개')});
  const sheet=document.querySelector('.private-sheet-box');if(sheet){uiRecordOpen=sheet.open;sheet.addEventListener('toggle',()=>{uiRecordOpen=sheet.open})}
  document.querySelectorAll('[data-mark]').forEach(b=>b.onclick=()=>{const [sec,o]=b.dataset.mark.split(':');priv.marks[sec]??={};const cur=priv.marks[sec][o]||'';const next=cur===''?'no':cur==='no'?'yes':'';priv.marks[sec][o]=next;savePrivate();b.classList.remove('no','yes');if(next)b.classList.add(next);const st=b.querySelector('.mkstate');if(st)st.textContent=next==='no'?'×':next==='yes'?'✓':''});
 }
@@ -226,7 +229,7 @@
 function startTheoryPoll(){if(theoryTimer)return;updateTheoryStatus();theoryTimer=setInterval(updateTheoryStatus,1900)}function stopTheoryPoll(){if(theoryTimer){clearInterval(theoryTimer);theoryTimer=null}}
 async function transitionTheoryPlacement(){if(state.phase!=='theory-select'||busy)return;const s=cloneState();s.phase='theory-place';s.theoryQueue=playerOrder(s);s.theoryPlaceIndex=0;s.current=s.theoryQueue[0];await commit(s,'가설 배치 시작')}
 function maybeAutoPlaceTheory(){if(!state||state.phase!=='theory-place')return;const q=state.theoryQueue||[],idx=Number(state.theoryPlaceIndex||0),who=q[idx];if(who!==mySeat()||theoryPlacing)return;const key=`${state.theoryRound}:${idx}:${who}`;if(theoryAutoKey===key)return;theoryAutoKey=key;setTimeout(()=>void placeTheory(key),120)}
-async function placeTheory(autoKey=''){if(theoryPlacing||state.phase!=='theory-place'||state.theoryQueue[state.theoryPlaceIndex]!==mySeat())return;theoryPlacing=true;try{const round=Number(state.theoryRound||0);const rows=await rpc('boardmate_planetx_place_theory_choice',{p_token:token(),p_room_id:roomId,p_phase_id:round});const s=cloneState();for(const r of rows||[]){if(!s.theories.some(t=>t.id===r.id))s.theories.push({...r,submitRound:round,faceUp:false})}s.theoryPlaceIndex++;logPublic(s,`${memberName(mySeat())} · 가설 ${rows?.length||0}개 자동 배치`);if(s.theoryPlaceIndex<s.theoryQueue.length){s.current=s.theoryQueue[s.theoryPlaceIndex];const ok=await commit(s,'가설 자동 배치');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';return}const advanced=await rpc('boardmate_planetx_advance_theories',{p_token:token(),p_room_id:roomId,p_phase_id:s.theoryRound});const byId=Object.fromEntries((advanced||[]).map(x=>[x.id,x]));s.theories=s.theories.map(t=>byId[t.id]?{...t,depth:byId[t.id].depth}:t);const review=await rpc('boardmate_planetx_review_due',{p_token:token(),p_room_id:roomId});applyReview(s,review);await rotateToNextTurn(s);const ok=await commit(s,'가설 단계 완료');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey=''}catch(e){if(autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';toast((e.message||e)+' · 가설 배치를 자동 재시도합니다.','error');setTimeout(()=>{if(state?.phase==='theory-place'){theoryAutoKey='';maybeAutoPlaceTheory()}},1500)}finally{theoryPlacing=false}}
+async function placeTheory(autoKey=''){if(theoryPlacing||state.phase!=='theory-place'||state.theoryQueue[state.theoryPlaceIndex]!==mySeat())return;theoryPlacing=true;try{const round=Number(state.theoryRound||0);const rows=await rpc('boardmate_planetx_place_theory_choice',{p_token:token(),p_room_id:roomId,p_phase_id:round});const s=cloneState();for(const r of rows||[]){if(!s.theories.some(t=>t.id===r.id))s.theories.push({...r,submitRound:round,faceUp:false})}s.theoryPlaceIndex++;logPublic(s,`${memberName(mySeat())} · 가설 ${rows?.length||0}개 자동 배치`);if(s.theoryPlaceIndex<s.theoryQueue.length){s.current=s.theoryQueue[s.theoryPlaceIndex];const ok=await commit(s,'가설 자동 배치');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';return}const advanced=await rpc('boardmate_planetx_advance_theories',{p_token:token(),p_room_id:roomId,p_phase_id:s.theoryRound});const byId=Object.fromEntries((advanced||[]).map(x=>[x.id,x]));s.theories=s.theories.map(t=>byId[t.id]?{...t,depth:byId[t.id].depth}:t);const review=await rpc('boardmate_planetx_review_due',{p_token:token(),p_room_id:roomId});applyReview(s,review);await rotateToNextTurn(s);const ok=await commit(s,'가설 단계 완료');if(!ok&&autoKey&&theoryAutoKey===autoKey)theoryAutoKey=''}catch(e){if(autoKey&&theoryAutoKey===autoKey)theoryAutoKey='';toast(e.message||e,'error')}finally{theoryPlacing=false}}
 function applyReview(s,r){const revealed=r?.revealed||[];for(const rv of revealed){const t=s.theories.find(x=>x.id===rv.id);if(t){Object.assign(t,{faceUp:true,object:rv.object,correct:rv.correct,removed:rv.correct===false})}}for(const c of r?.confirmed||[]){s.confirmed[c.sector]=c.object;logPublic(s,`교차 검증 · 섹터 ${c.sector} = ${objLabel(c.object)} 확인`)}for(const seat of r?.penalties||[]){const p=s.players[Number(seat)];if(p){p.time+=1;p.tie=++s.tieSeq;logPublic(s,`${memberName(Number(seat))} · 틀린 가설 시간 +1`)}}if(revealed.length&&!r?.confirmed?.length)logPublic(s,`교차 검증 · ${revealed.length}개 가설이 틀림으로 판정됨`)}
 
 function openFinalTheory(){const item=state.finalQueue[state.finalIndex],max=item.distance<=3?1:2;modal(`<h2>🏁 마지막 가설 제출</h2><p class="sub">최대 ${max}개. 이 최종 기회의 가설들은 채점상 동시에 제출된 것으로 처리됩니다.</p>${Array.from({length:max},(_,i)=>`<div class="form-row"><div class="field"><label>개체</label><select data-f-obj><option value="">제출 안 함</option>${optionObjects(false)}</select></div><div class="field"><label>섹터</label><select data-f-sec>${optionSectors()}</select></div></div>`).join('')}<div class="modal-actions"><button class="btn ghost" data-close>취소</button><button class="btn green" id="doFinalTheory">제출</button></div>`,m=>{m.querySelector('[data-close]').onclick=closeModal;m.querySelector('#doFinalTheory').onclick=async()=>{const os=[...m.querySelectorAll('[data-f-obj]')],ss=[...m.querySelectorAll('[data-f-sec]')],choices=os.map((o,i)=>o.value?{object:o.value,sector:Number(ss[i].value)}:null).filter(Boolean);if(new Set(choices.map(x=>`${x.sector}:${x.object}`)).size!==choices.length){toast('같은 섹터에 같은 종류의 가설 토큰을 2개 제출할 수 없습니다.','error');return}try{const rows=await rpc('boardmate_planetx_final_submit',{p_token:token(),p_room_id:roomId,p_choices:choices,p_max:max});closeModal();const s=cloneState();for(const r of rows||[])s.theories.push({...r,submitRound:'최종',faceUp:false});logPublic(s,`${memberName(mySeat())} · 마지막 가설 ${rows?.length||0}개 제출`);advanceFinal(s);await commit(s,'마지막 가설')}catch(e){toast(e.message||e,'error')}}})}
@@ -237,7 +240,7 @@
 
 async function boot(){
  if(!configured()||!roomId)showFatal('Supabase 설정 또는 room 파라미터가 없습니다.');
- try{me=await getMe();if(!me)showFatal('로그인이 필요합니다.');({room,members}=await loadRoom());members=[...members].sort((a,b)=>Number(a.seat)-Number(b.seat));if(room.game!=='planetx')showFatal('행성 X 방이 아닙니다.');if(!members.some(m=>m.user_id===me.user_id))showFatal('이 방의 참가자가 아닙니다.');loadPrivate();const row=await loadState();if(row){revision=Number(row.revision||0);state=row.state}else{const status=await rpc('boardmate_planetx_status',{p_token:token(),p_room_id:roomId}).catch(()=>null);if(status?.started){state=makeInitial(status.mode,status.code);revision=await saveState(Math.max(0,revision),state)}}if(state)await ensureResearchTopics();render();startStatePoll(async row=>{if(Number(row.revision)<=revision)return;revision=Number(row.revision);state=row.state;render()},{interval:5000});await subscribeRoomBroadcast('planetx_theory_ready',()=>void updateTheoryStatus());setSync('동기화됨')
+ try{me=await getMe();if(!me)showFatal('로그인이 필요합니다.');({room,members}=await loadRoom());members=[...members].sort((a,b)=>Number(a.seat)-Number(b.seat));if(room.game!=='planetx')showFatal('행성 X 방이 아닙니다.');if(!members.some(m=>m.user_id===me.user_id))showFatal('이 방의 참가자가 아닙니다.');await loadPrivate();const row=await loadState();if(row){revision=Number(row.revision||0);state=row.state}else{const status=await rpc('boardmate_planetx_status',{p_token:token(),p_room_id:roomId}).catch(()=>null);if(status?.started){state=makeInitial(status.mode,status.code);revision=await saveState(Math.max(0,revision),state)}}if(state)await ensureResearchTopics();render();startStatePoll(async row=>{if(Number(row.revision)<=revision)return;revision=Number(row.revision);state=row.state;render()},{interval:5000});await subscribeRoomBroadcast('planetx_theory_ready',()=>void updateTheoryStatus());setSync('동기화됨')
  }catch(e){showFatal((e.message||e)+(String(e.message||'').includes('boardmate_planetx')?' · SUPABASE_PLANETX_PVP_V1.sql을 먼저 실행하세요.':''))}
 }
 boot();
--- /mnt/data/arena_work/index.html	2026-09-09 14:36:36.899942470 +0000
+++ /mnt/data/arena_v44_work/index.html	2026-09-09 14:40:58.882489832 +0000
@@ -13,6 +13,6 @@
   <div id="app"></div>
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   <script src="./config.js"></script>
-  <script type="module" src="./app.js?v=43"></script>
+  <script type="module" src="./app.js?v=44"></script>
 </body>
 </html>
--- /mnt/data/arena_work/sw.js	2026-09-09 14:18:28.092193935 +0000
+++ /mnt/data/arena_v44_work/sw.js	2026-09-09 14:40:58.882540497 +0000
@@ -1,4 +1,4 @@
-const CACHE='boardmate-shell-v11.4.43';
+const CACHE='boardmate-shell-v11.4.44';
 self.addEventListener('install',()=>self.skipWaiting());
 self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('boardmate-shell-')&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
 self.addEventListener('fetch',event=>{
