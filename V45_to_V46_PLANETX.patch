--- /mnt/data/work_arena/release45/online-planetx.html	2026-09-10 00:18:56.366789462 +0000
+++ /mnt/data/work_arena/release46/online-planetx.html	2026-09-10 00:28:07.511140444 +0000
@@ -4,7 +4,7 @@
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
 <title>BoardMate Online · 행성 X를 찾아서</title>
-<!-- BoardMate Arena v11.4.45 · Planet X board events + paper terminology + bottom references -->
+<!-- BoardMate Arena v11.4.46 · Planet X circular private record sheet -->
 <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
 <script src="./config.js"></script>
 <style>
@@ -29,6 +29,41 @@
 
 /* v11.4.45 · circular-board academic festival/conference markers + bottom references */
 .orbit-wrap{padding:22px 24px 16px}.orbit-board{overflow:visible}.orbit-event-marker{position:absolute;transform:translate(-50%,-50%);min-width:48px;min-height:25px;padding:3px 6px;border-radius:999px;display:flex;align-items:center;justify-content:center;gap:3px;z-index:7;font-size:11px;font-weight:950;line-height:1;background:#17243aee;border:1px solid #5d7692;box-shadow:0 3px 10px #0009;white-space:nowrap}.orbit-event-marker small{font-size:8px;font-weight:950}.orbit-event-marker.festival{color:#ffe49a;border-color:#cba84e;background:#3b3017ee}.orbit-event-marker.conference-event{color:#bfeeff;border-color:#55b8d9;background:#12364aee}.orbit-event-schedule{display:flex;gap:7px;flex-wrap:wrap;justify-content:center;margin:-5px 0 9px;font-size:10px}.orbit-event-schedule span{border:1px solid #304b64;border-radius:999px;padding:4px 8px;background:#081523;color:#c7dbea}.orbit-event-schedule b{color:#f3fbff}.reference-bottom{scroll-margin-top:70px}.reference-bottom>h3{margin-bottom:10px}@media(max-width:520px){.orbit-wrap{padding:18px 18px 14px}.orbit-event-marker{min-width:38px;min-height:21px;padding:2px 4px;font-size:9px}.orbit-event-marker small{font-size:7px}.orbit-event-schedule{font-size:9px}}
+
+
+/* v11.4.46 · circular private record sheet based on the physical 12/18-sector note sheets */
+.record-wheel-wrap{display:grid;place-items:center;margin:10px 0 4px;padding:8px 4px 14px;overflow:visible}
+.record-wheel{--record-ring:#090d14;position:relative;width:min(610px,100%);aspect-ratio:1;border-radius:50%;background:radial-gradient(circle at center,#263347 0 28%,#111b2b 28.5% 82%,var(--record-ring) 82.5% 100%);border:1px solid #41536d;box-shadow:inset 0 0 44px #0009,0 16px 34px #0005;isolation:isolate;touch-action:manipulation}
+.record-wheel.expert{width:min(660px,100%)}
+.record-wheel::before,.record-wheel::after{content:'';position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);border-radius:50%;pointer-events:none;z-index:1}
+.record-wheel::before{width:72.4%;height:72.4%;border:1px solid #6c7e944f}
+.record-wheel::after{width:29.4%;height:29.4%;border:1px solid #7f8fa44f;background:radial-gradient(circle at 38% 34%,#43526a,#253247 66%,#172234 100%);box-shadow:inset 0 0 20px #0008}
+.record-spoke{position:absolute;left:50%;top:50%;width:1px;height:41.2%;transform-origin:50% 0;background:#7990a45c;z-index:1;pointer-events:none}
+.record-sector-no{position:absolute;transform:translate(-50%,-50%);z-index:4;min-width:26px;height:24px;padding:0 5px;border-radius:999px;display:grid;place-items:center;color:#f7fbff;font-size:12px;font-weight:950;line-height:1;text-shadow:0 1px 2px #000}
+.record-wheel.expert .record-sector-no{min-width:21px;height:20px;font-size:10px}
+.record-mark{position:absolute;transform:translate(-50%,-50%);z-index:5;width:27px;height:27px;padding:0;border:1px solid #53677f;border-radius:50%;background:#0a1523e8;color:#d9e8f7;display:grid;place-items:center;cursor:pointer;box-shadow:0 2px 6px #0008;transition:transform .12s ease,background .12s ease,border-color .12s ease,color .12s ease}
+.record-mark:hover{transform:translate(-50%,-50%) scale(1.1);border-color:#8db9d8}
+.record-mark:focus-visible{outline:2px solid var(--gold);outline-offset:2px}
+.record-mark-icon{font-size:16px;line-height:1;filter:saturate(.78)}
+.record-mark .mkstate{position:absolute;right:-5px;bottom:-5px;min-width:15px;height:15px;padding:0 2px;border-radius:999px;display:grid;place-items:center;background:#111a28;border:1px solid #61738a;color:#dce8f5;font-size:10px;font-weight:950;line-height:1;box-shadow:0 1px 4px #000a}
+.record-mark .mkstate:empty{display:none}
+.record-mark.no{border-color:#d26a7a;background:#351824;color:#ffb1bd;box-shadow:0 0 0 1px #ff7a8833,0 2px 6px #0008}
+.record-mark.no .mkstate{background:#7b2736;border-color:#ff8b9c;color:#fff}
+.record-mark.yes{border-color:#58d99a;background:#12372b;color:#8df3bd;box-shadow:0 0 0 1px #62df9b33,0 2px 6px #0008}
+.record-mark.yes .mkstate{background:#17613f;border-color:#7cf0b5;color:#fff}
+.record-mark.comet-mark{border-style:dashed;border-color:#7b6d9e}
+.record-mark.comet-mark .record-mark-icon{filter:none}
+.record-wheel.expert .record-mark{width:23px;height:23px;border-radius:7px}
+.record-wheel.expert .record-mark-icon{font-size:12px}
+.record-wheel.expert .record-mark .mkstate{right:-4px;bottom:-5px;min-width:13px;height:13px;font-size:8px}
+.record-wheel-center{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);z-index:6;width:27%;aspect-ratio:1;border-radius:50%;display:grid;place-items:center;align-content:center;text-align:center;padding:9px;color:#dbe8f7;pointer-events:none}
+.record-wheel-center strong{font-size:12px;color:#fff;line-height:1.25}
+.record-wheel-center span{display:block;margin-top:3px;font-size:10px;color:#d9c9ff;font-weight:900}
+.record-wheel-center small{display:block;margin-top:4px;font-size:8px;line-height:1.4;color:#98aec4}
+.record-sheet-tip{text-align:center;font-size:10px;color:#a99bc7;margin:0 0 5px;line-height:1.45}
+@media(max-width:700px){.record-wheel-wrap{padding-inline:0}.record-wheel,.record-wheel.expert{width:min(100%,560px)}}
+@media(max-width:520px){.record-wheel{width:min(100%,430px)}.record-wheel.expert{width:min(100%,455px)}.record-mark{width:clamp(15px,4.7vw,22px);height:clamp(15px,4.7vw,22px)}.record-mark-icon{font-size:clamp(9px,3.2vw,12px)}.record-mark .mkstate{right:-4px;bottom:-4px;min-width:11px;height:11px;font-size:7px}.record-wheel.expert .record-mark{width:clamp(12px,4vw,18px);height:clamp(12px,4vw,18px);border-radius:5px}.record-wheel.expert .record-mark-icon{font-size:clamp(8px,2.7vw,10px)}.record-wheel.expert .record-mark .mkstate{right:-3px;bottom:-3px;min-width:9px;height:9px;font-size:6px}.record-sector-no{font-size:10px;min-width:22px;height:20px}.record-wheel.expert .record-sector-no{font-size:8px;min-width:18px;height:17px}.record-wheel-center strong{font-size:10px}.record-wheel-center span{font-size:8px}.record-wheel-center small{font-size:7px}}
+
 </style>
 </head>
 <body>
@@ -130,7 +165,7 @@
     s.theoryRound=Number(s.theoryRound||0)+1;s.phase='theory-select';s.theoryPlaceIndex=0;s.theoryQueue=[];s.current=activeSeat(s);logPublic(s,`논문 단계 ${s.theoryRound} 시작`);return s;
   }
 }
-function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.45',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
+function makeInitial(mode,code){const order=[...members.keys()];for(let i=order.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[order[i],order[j]]=[order[j],order[i]]}const rank=Object.fromEntries(order.map((x,i)=>[x,i]));return{kind:'planetx-pvp-v1',version:'11.4.46',mode,code,phase:'setup',earth:1,current:0,tieSeq:members.length,theoryRound:0,theoryPlaceIndex:0,theoryQueue:[],players:members.map((m,i)=>({seat:i,name:memberName(i),time:1,tie:rank[i],targetUsed:0,lastAction:'',xScore:0})),conferences:{},confirmed:{},theories:[],log:[{at:new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'}),text:`게임 ${code} · ${mode==='expert'?'전문가':'표준'} 모드 준비`}],finalQueue:[],finalIndex:0,firstLocator:null,scores:null,over:false}}
 
 function captureScroll(){const inner={};document.querySelectorAll('[data-scroll-key]').forEach(el=>{inner[el.dataset.scrollKey]={top:el.scrollTop,left:el.scrollLeft}});return{x:window.scrollX,y:window.scrollY,inner}}
 function restoreScroll(snap){if(!snap)return;requestAnimationFrame(()=>{window.scrollTo(snap.x,snap.y);document.querySelectorAll('[data-scroll-key]').forEach(el=>{const p=snap.inner?.[el.dataset.scrollKey];if(p){el.scrollTop=p.top;el.scrollLeft=p.left}})})}
@@ -216,7 +251,8 @@
 function theorySelectPanel(){const status=state.theoryStatus||[],mine=status.find(x=>x.seat===mySeat());return`<div class="notice">모든 플레이어가 동시에 논문을 비공개로 선택합니다. ${state.mode==='expert'?'최대 2개':'최대 1개'}이며 0개 제출도 가능합니다.</div><button id="chooseTheory" class="btn ${mine?.ready?'ghost':'green'}">${mine?.ready?'내 선택 수정':'논문 비공개 선택'}</button><div class="theory-status">${members.map((m,i)=>{const x=status.find(a=>a.seat===i);return`<span class="badge ${x?.ready?'gold':''}" style="border:1px solid ${playerColor(i)}66"><span style="color:${playerColor(i)}">●</span> ${x?.ready?'✓':'…'} ${esc(memberName(i))}</span>`}).join('')}</div>`}
 function finalPanel(){const item=state.finalQueue?.[state.finalIndex];if(!item)return`<button id="finishReveal" class="btn gold">정답 공개 · 점수 계산</button>`;if(item.seat!==mySeat())return`<div class="notice"><b>${playerNameHtml(item.seat)}</b>의 마지막 점수 기회입니다.<br>선두보다 ${item.distance}칸 뒤였습니다.</div>`;const max=item.distance<=3?1:2;return`<div class="notice"><b>마지막 점수 기회</b><br>선두보다 ${item.distance}칸 뒤 · 논문 ${max}개 제출 <b>또는</b> 행성 X 찾기를 시도할 수 있습니다. 이때 시간은 이동하지 않습니다.</div><div class="actions"><button id="finalTheory" class="btn green">논문 최대 ${max}개 제출</button><button id="finalLocate" class="btn gold">행성 X 찾기</button></div>`}
 function privateObservationPanel(){const rows=priv.observations||[];return`<section class="private-quick"><div class="private-quick-head"><h3>🔒 내 비공개 조사 기록</h3><span class="badge">${rows.length}건</span></div><div class="record-tip">조사 결과가 최신순으로 바로 쌓입니다. 보드 아래에서 계속 확인할 수 있습니다.</div><div class="obs quick-obs" data-scroll-key="private-observations" style="margin-top:7px">${rows.map(o=>`<div class="line"><b>${esc(o.at)} · ${esc(o.type)}</b><br>${esc(displayKo(o.text))}</div>`).join('')||'<div class="sub">아직 조사 기록이 없습니다.</div>'}</div></section>`}
-function privatePanel(){const allowedComets=new Set(cometSectors());const marks=Array.from({length:sectorCount()},(_,i)=>i+1).map(sec=>{const objects=['asteroid',...(allowedComets.has(sec)?['comet']:[]),'dwarf','gas','empty','x'];return`<div class="note-cell ${allowedComets.has(sec)?'comet-possible':''}"><b>${sec}</b>${allowedComets.has(sec)?'<span class="comet-possible-label">🌠 혜성 가능</span>':''}<div class="mark-row">${objects.map(o=>{const v=priv.marks?.[sec]?.[o]||'';return`<button class="mark ${v}" title="${esc(objLabel(o))}" data-mark="${sec}:${o}">${OBJNOTE[o]}<span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}</div></div>`}).join('');return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">계정에 저장된 시작 정보가 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><h3 style="margin-top:14px">📝 섹터 메모</h3><div class="sub" style="margin-bottom:7px">미정 → × 없음 → ✓ 있음. 혜성은 가능한 섹터에만 버튼이 표시됩니다.</div><div class="note-grid">${marks}</div>`}
+function recordPoint(angleDeg,radiusPct){const rad=angleDeg*Math.PI/180;return{x:50+Math.sin(rad)*radiusPct,y:50-Math.cos(rad)*radiusPct}}
+function privatePanel(){const n=sectorCount(),expert=n===18,step=360/n,sectorOneAngle=expert?100:105,allowedComets=new Set(cometSectors());const radii=expert?{number:46.0,x:39.4,empty:34.7,gas:30.0,dwarf:25.3,asteroid:20.6,comet:15.9}:{number:46.0,x:39.7,empty:35.0,gas:30.3,dwarf:25.6,asteroid:20.9,comet:16.2};const spokes=Array.from({length:n},(_,i)=>{const a=sectorOneAngle+(i*step)-(step/2);return`<i class="record-spoke" style="transform:translateX(-50%) rotate(${a+180}deg)"></i>`}).join('');const sectorMarks=Array.from({length:n},(_,i)=>i+1).map(sec=>{const angle=sectorOneAngle+(sec-1)*step,num=recordPoint(angle,radii.number),objects=['x','empty','gas','dwarf','asteroid',...(allowedComets.has(sec)?['comet']:[])];return`<span class="record-sector-no" style="left:${num.x.toFixed(3)}%;top:${num.y.toFixed(3)}%">${sec}</span>${objects.map(o=>{const p=recordPoint(angle,radii[o]),v=priv.marks?.[sec]?.[o]||'',label=objLabel(o);return`<button type="button" class="record-mark ${o==='comet'?'comet-mark ':''}${v}" style="left:${p.x.toFixed(3)}%;top:${p.y.toFixed(3)}%" title="${esc(label)} · 미정/없음/있음 전환" aria-label="${sec}섹터 ${esc(label)} 기록" data-mark="${sec}:${o}"><span class="record-mark-icon">${OBJICON[o]}</span><span class="mkstate">${v==='no'?'×':v==='yes'?'✓':''}</span></button>`}).join('')}`}).join('');const modeLabel=expert?'전문가 · 18섹터':'표준 · 12섹터';return`<div style="margin:10px 0"><b>${SEASON_KO[priv.season]||'시작 정보'}</b> · ${DIFF[priv.difficulty]?.[0]||'미선택'}</div><div class="hints" data-scroll-key="private-hints">${(priv.hints||[]).map(h=>`<div class="line">섹터 <b>${h.sector}</b>에는 <b>${esc(nounKo(h.not))}</b>이(가) 없습니다.</div>`).join('')||'<div class="sub">계정에 저장된 시작 정보가 없습니다. 같은 방에서 준비 단계의 시작 정보를 다시 받으세요.</div>'}</div><h3 style="margin-top:14px">📝 원형 섹터 기록지</h3><div class="record-sheet-tip">실물 기록지처럼 바깥쪽 섹터 번호에서 안쪽으로 개체 아이콘을 배치했습니다. 아이콘을 누르면 미정 → × 없음 → ✓ 있음으로 바뀝니다. 혜성은 가능한 섹터에만 표시됩니다.</div><div class="record-wheel-wrap"><div class="record-wheel ${expert?'expert':'standard'}" style="--sector-count:${n}">${spokes}${sectorMarks}<div class="record-wheel-center"><strong>섹터 메모</strong><span>${SEASON_KO[priv.season]||modeLabel}</span><small>${modeLabel}<br>× 없음 · ✓ 있음</small></div></div></div>`}
 
 function scorePanel(){const scores=state.scores||[];return`<h2>🏆 최종 점수</h2><table class="score-table"><thead><tr><th>플레이어</th><th>논문</th><th>리더</th><th>행성 X</th><th>합계</th></tr></thead><tbody>${scores.map(x=>`<tr><td style="border-left:4px solid ${playerColor(x.seat)}">${x.rank===1?'🏆 ':''}<span style="color:${playerColor(x.seat)}">●</span> ${esc(memberName(x.seat))} <span class="player-color-name" style="--pc:${playerColor(x.seat)}">${playerColorName(x.seat)}</span></td><td>${x.theory}</td><td>${x.leader}</td><td>${x.x}</td><td><b>${x.total}</b></td></tr>`).join('')}</tbody></table>${state.sharedTie?'<div class="notice" style="margin-top:10px">공식 타이브레이커 후에도 완전 동점이라 공동 승리입니다. 이 경우 BoardMate ELO는 자동 반영하지 않습니다.</div>':''}${endPanel('게임 종료',scores.filter(x=>x.rank===1).map(x=>memberName(x.seat)).join(', ')+' 승리')}`}
 
--- /mnt/data/work_arena/release45/sw.js	2026-09-10 00:17:56.075530254 +0000
+++ /mnt/data/work_arena/release46/sw.js	2026-09-10 00:24:40.073504305 +0000
@@ -1,4 +1,4 @@
-const CACHE='boardmate-shell-v11.4.45';
+const CACHE='boardmate-shell-v11.4.46';
 self.addEventListener('install',()=>self.skipWaiting());
 self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('boardmate-shell-')&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
 self.addEventListener('fetch',event=>{
--- /mnt/data/work_arena/release45/index.html	2026-09-10 00:17:56.076018429 +0000
+++ /mnt/data/work_arena/release46/index.html	2026-09-10 00:24:40.073560879 +0000
@@ -13,6 +13,6 @@
   <div id="app"></div>
   <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
   <script src="./config.js"></script>
-  <script type="module" src="./app.js?v=45"></script>
+  <script type="module" src="./app.js?v=46"></script>
 </body>
 </html>
--- /mnt/data/work_arena/release45/HANDOFF_VERSION.txt	2026-09-10 00:17:56.076264083 +0000
+++ /mnt/data/work_arena/release46/HANDOFF_VERSION.txt	2026-09-10 00:24:40.073589772 +0000
@@ -1 +1 @@
-11.4.45
+11.4.46
